# Billiard Hub — project rules

## استان و شهر — منبعِ واحد (single source of truth)

هرجا در این پروژه از کاربر خواسته می‌شود **شهر** (و در صورت نیاز **استان**) را وارد یا
انتخاب کند، باید **حتماً** از کامپوننت `ProvinceCitySelect` استفاده شود و **هرگز** لیست
استان‌ها/شهرها به‌صورت دستی یا مجدد ساخته نشود.

- **کامپوننت:** `apps/web/components/ProvinceCitySelect.tsx`
  (دراپ‌داونِ زنجیره‌ایِ استان → شهر، سرچ‌دار، controlled).
- **داده:** `apps/web/data/iran-geo.json` (۳۱ استان، ۱۱۹۳ شهر — از
  `sajaddp/list-of-cities-in-Iran`, نسخه‌ی `cities-filtered`).
- **لودر:** `apps/web/lib/iran-geo.ts` — `getProvinces()`, `getProvinceNames()`,
  `getCities(province)`, `provinceOfCity(city)`.

### قواعد
1. برای گرفتنِ شهر از کاربر، `<input>` متنی یا `<select>` با لیستِ هاردکد **نساز**.
   به‌جایش:
   ```tsx
   import ProvinceCitySelect from '@/components/ProvinceCitySelect' // یا مسیر نسبی
   const [geo, setGeo] = useState({ province: '', city: '' })
   <ProvinceCitySelect value={geo} onChange={setGeo} required />
   ```
2. اگر فرم قبلاً فقط `city` را نگه می‌داشت، یک فیلد `province` هم کنارش اضافه کن و هر
   دو را به همان state/store قبلی وصل کن. برای داده‌ی قدیمیِ فقط‌شهر، استان را با
   `provinceOfCity(city)` بک‌فیل کن.
3. لیستِ شهرِ هاردکد در هیچ فایلِ جدیدی مجاز نیست. لیست‌های قدیمی (مثلاً `CITIES`،
   `iranCities`، `IRAN_PROVINCES`) در حال حذف‌اند — اگر به یکی برخوردی، با
   `ProvinceCitySelect` جایگزینش کن.
4. فیلترهای لیست (مثلاً «فیلتر فروشگاه‌ها بر اساس شهر») ورودیِ کاربر نیستند و اجباری
   نیست از این کامپوننت استفاده کنند، ولی ترجیحاً از `getCities`/`getProvinceNames`
   همین لودر بخوانند تا لیست‌ها یکی بمانند.

---

## Stack

- **مونوریپو:** Turborepo + npm workspaces (`apps/web`، `apps/api`، `packages/*`).
  پکیج‌منیجر **npm** است (`package-lock.json`). از pnpm/yarn استفاده نکن.
- **فرانت:** `apps/web` — Next.js 16 App Router · React 19 · TypeScript · Tailwind 3.4 · RTL فارسی
- **بک‌اند:** `apps/api` — NestJS
- **پکیج‌های مشترک:** `@repo/ui`، `@repo/eslint-config`، `@repo/typescript-config`
- محیط توسعه: **Windows / PowerShell**. دستورها را با سینتکس PowerShell بنویس
  (نه `&&`، نه `rm -rf`).

### دستورها
```powershell
npm run dev     # turbo run dev — وب روی پورت ۳۰۰۰
npm run build   # بیلد پروداکشن — قبل از هر دیپلوی باید سبز باشد
npm run lint
```

---

## استقرار (Deployment)

⚠️ این پروژه **زنده و در حال استفاده** است. حدود ۷۰٪ تکمیل شده و روی دامنه‌ی واقعی سرو می‌شود.
هر تغییری با این فرض انجام شود که کاربر واقعی دارد.

- میزبانی روی **سرور اختصاصی (VPS)** پشت **nginx**. اسکریپت‌های مرتبط در ریشه:
  `deploy.sh`، `nginx-canonical.sh`، `nginx-perf.sh`، `Dockerfile`.
- **Vercel و Netlify دیگر استفاده نمی‌شوند.** فایل‌های `vercel.json`، `netlify.toml`
  و پوشه‌ی `.vercel/` باقی‌مانده‌ی مهاجرت‌اند — به آن‌ها استناد نکن و بر اساسشان
  پیشنهاد نده.
- **سرور:** `root@130.185.72.87` · مسیر `/opt/billiardhub` · سرویس systemd به نام
  `billiardhub`. فقط `apps/web` (پورت ۳۰۰۰) بیلد و ری‌استارت می‌شود.
- **دیتابیس: PostgreSQL داخل Supabase self-hosted، در داکر روی همان VPS**
  (کانتینر `supabase-db`). دسترسی از کد با `sb()` در `lib/finance/db` و PostgREST.
  - ⚠️ **Supabase ابری (supabase.co) استفاده نمی‌شود.** هیچ پیشنهادی بر اساس داشبورد
    یا CLI ابری Supabase نده. در مقابل، پوشه‌ی `supabase/` **زنده و معتبر** است —
    پاکش نکن و منسوخ فرضش نکن.
  - مهاجرت‌ها روی سرور اجرا می‌شوند:
    `docker exec -i supabase-db psql -U postgres -d postgres`
  - `deploy.sh` هیچ مهاجرتی اجرا نمی‌کند. هر تغییر اسکیما دستی و با تأیید صریح من است.
- `Dockerfile` فقط `apps/api` را می‌سازد و **در مسیر دیپلوی فعلی استفاده نمی‌شود** —
  بر اساسش تصمیم نگیر.
- **رشته‌ی اتصال دیتابیس فقط در `apps/web/.env.local` روی سرور است.** `deploy.sh` با
  `--exclude='.env*'` آن را دست‌نخورده نگه می‌دارد. هرگز `.env*` سرور را بازنویسی نکن.
- متغیرهای محیطی در `.env.local` (توسعه) و `.env.production`. **هیچ‌کدام هرگز کامیت نشوند.**

### قواعد دیپلوی
1. قبل از هر دیپلوی، `npm run build` باید بدون خطا تمام شود.
2. هرگز خودت دیپلوی نکن. اگر کار آماده‌ی دیپلوی است، فقط بگو.
3. تغییر در `nginx-*.sh`، `Dockerfile` یا `deploy.sh` فقط با تأیید صریح من.
4. هیچ دستور مخربی روی فایل‌های سرور یا دیتابیس اجرا نکن (drop، truncate، migration
   بازگشت‌ناپذیر) بدون پرسیدن.

---

## مستندات

- **`CLAUDE.md` تنها منبع معتبر است.**
- `PROJECT_ARCHITECTURE.md` و `README.md` ممکن است قدیمی باشند — اگر با این فایل تناقض
  داشتند، این فایل درست است.
- فایل‌های داخل `docs/archive/` **منسوخ‌اند** — نخوان و به آن‌ها استناد نکن.

---

## قواعد کلی کدنویسی

1. **کدی که `tsc --noEmit` را رد نکند، تمام‌شده نیست.** `any` ممنوع؛ `@ts-ignore` فقط با یک خط توضیح دلیل.
2. **قبل از نوشتن، بخوان.** قبل از ساخت هر کامپوننت/یوتیل جدید، `apps/web/components`، `apps/web/lib` و `packages/ui` را بگرد. دوباره‌کاری باگ است.
3. **API فریم‌ورک را از حافظه ننویس.** Next.js 16 و React 19 تازه‌اند — قبل از استفاده از هر API، مستندات نسخه‌ای را با Context7 بگیر.
4. **کار UI بدون دیدن، تمام نیست.** بعد از هر تغییر ظاهری، با Playwright MCP صفحه را باز کن، اسکرین‌شات بگیر و تأیید کن. «انجام شد» بدون دیدن خروجی قابل قبول نیست.
5. **هیچ dependency جدیدی بدون پرسیدن** اضافه نکن. پروژه لایو است؛ نصب بی‌مورد ریسک دارد.
6. مرز `apps/web` و `apps/api` را نشکن: تایپ مشترک در `packages/`، نه import مستقیم بین دو اپ.
7. روی کدی که خارج از محدوده‌ی تسک است دست نزن. رفکتور خودسرانه ممنوع.

---

## قواعد RTL (فارسی)

این پروژه راست‌به‌چپ است. رعایت نشدن این‌ها بزرگ‌ترین منبع باگ ظاهری است:

- **همیشه logical property**، هرگز directional:
  - `ps-*` / `pe-*` به‌جای `pl-*` / `pr-*`
  - `ms-*` / `me-*` به‌جای `ml-*` / `mr-*`
  - `text-start` / `text-end` به‌جای `text-left` / `text-right`
  - `start-0` / `end-0` به‌جای `left-0` / `right-0`
  - `border-s` / `border-e` به‌جای `border-l` / `border-r`
- آیکون‌های جهت‌دار (فلش «بعدی/قبلی»، chevron) در RTL باید معکوس شوند.
- اعداد: در متن فارسی از ارقام فارسی، در فیلدهای عددی/تاریخ/قیمت از لاتین. یک هلپر واحد برای فرمت اعداد و تاریخ شمسی، نه فرمت‌کردن دستی در کامپوننت.
- `dir="rtl"` روی `<html>` تنظیم شده — روی کامپوننت‌ها تکرارش نکن مگر برای بلاک لاتین (کد، ایمیل، URL) که باید `dir="ltr"` بگیرد.
- فونت فارسی: از همان فونت پروژه استفاده کن، فونت جدید اضافه نکن.

---

## قواعد ظاهری (UI)

### استایل
- **Tailwind اولویت اول است.** `style={{...}}` فقط برای مقادیر داینامیک محاسبه‌شده (مثلاً عرض progress bar). استایل ثابت هرگز inline نباشد.
- استایل تکراری در چند جا ⟵ به کامپوننت تبدیلش کن.
- پروژه ظاهر liquid glass دارد؛ افکت شیشه‌ای موجود را در یک کلاس/کامپوننت مشترک نگه دار، در هر صفحه از نو نساز.

### اسپیسینگ
فقط این مقادیر: `1, 2, 3, 4, 6, 8, 12, 16, 24`. مقدار دلخواه مثل `p-[13px]` ممنوع — اگر مقیاس جواب نمی‌دهد، لِی‌اوت اشتباه است.

### تایپوگرافی
| نقش | کلاس |
|---|---|
| عنوان صفحه | `text-3xl font-semibold` |
| عنوان بخش | `text-xl font-semibold` |
| متن | `text-sm` |
| متادیتا | `text-xs text-gray-500` |

در هر صفحه حداکثر ۳ اندازه فونت دیده شود.

### رنگ
- پالت پروژه در `tailwind.config.ts` تعریف شده — از همان استفاده کن. مرجع رنگ‌ها: `Color code.txt`.
- در هر صفحه یک رنگ تأکید. رنگ معنا دارد؛ تزئین با خنثی‌ها انجام می‌شود.

### کامپوننت
- هر المان تعاملی باید چهار حالت داشته باشد: hover، focus-visible، disabled، loading.
- هر لیست باید سه حالت داشته باشد: skeleton لودینگ، حالت خالی، حالت خطا. صفحه‌ای که این سه را ندارد ناقص است.
- موبایل‌فرست. هر صفحه باید در عرض ۳۷۵px درست باشد.
- کامپوننت بالای ~۱۵۰ خط را بشکن.

### دسترس‌پذیری
- HTML معنایی: `<button>`، `<nav>`، `<main>`. `<div>` کلیک‌شونده ممنوع.
- هر input یک `<label>`؛ هر دکمه‌ی فقط‌آیکون یک `aria-label`.
- حلقه‌ی focus قابل دیدن روی همه‌ی المان‌های تعاملی.

---

## قواعد Next.js 16 / React 19

- **پیش‌فرض Server Component.** `"use client"` فقط وقتی state/effect/API مرورگر لازم است — و تا حد ممکن پایین درخت، نه بالای صفحه.
- فچ اولیه‌ی داده در Server Component یا Route Handler. هرگز در `useEffect`.
- Mutation از طریق Server Action + `revalidatePath`/`revalidateTag`.
- فقط `async/await`. زنجیره‌ی `.then()` ننویس.
- هر ورودی خارجی (فرم، body، params) در مرز با Zod اعتبارسنجی شود.
- خطا: تایپ‌دار throw کن، در `error.tsx` هندل کن. `catch {}` خالی ممنوع.
- `localStorage` فقط در client و با گارد `typeof window !== 'undefined'`.

## ساختار و نام‌گذاری
- کامپوننت: `PascalCase.tsx` — بقیه: `kebab-case.ts`
- منطق خالص در `lib/` (بدون React)، هوک‌ها در `hooks/`
- export نام‌دار، جز فایل‌های ویژه‌ی Next (`page.tsx`, `layout.tsx`)

## روند کار
- قبل از تسک غیرجزئی: پلن کوتاه بده و منتظر تأیید بمان.
- بعد از اتمام: lint، typecheck و build را اجرا کن و **خروجی واقعی** را گزارش بده. نتیجه را حدس نزن.
- قبل از هر commit، ساب‌ایجنت `code-reviewer` را اجرا کن.
- کامیت: conventional (`feat:`, `fix:`, `refactor:`)، کوچک و اتمیک.
- `.env`، `.env.local`، `.env.production` هرگز کامیت نشوند.
