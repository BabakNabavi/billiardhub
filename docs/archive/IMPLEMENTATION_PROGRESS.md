# Billiard Hub Advertising System Progress

# SECURITY CREDENTIAL ROTATION — ✅ COMPLETED (2026-07-29)

> **هر چهار اعتبارنامه چرخانده و راستی‌آزمایی شد.** چرخش‌ها را مالک به‌صورت دستی در Supabase و Vercel انجام داد؛ راستی‌آزمایی با آزمونِ زنده انجام شد.

| اعتبارنامه | وضعیت | اثباتِ زنده |
|---|---|---|
| Supabase `service_role` | ✅ چرخید | کلیدِ لو رفته روی `users`/`sessions`/`app_settings` در ۶ دورِ پیاپی **۴۰۱** |
| Supabase `anon` → publishable | ✅ چرخید | باندلِ زنده `sb_publishable_…` دارد؛ کلیدِ legacy قبلی **۴۰۱** |
| رمزِ دیتابیس | ✅ چرخید | اتصال با رمزِ قدیمی: `password authentication failed`؛ با رمزِ تازه موفق |
| `JWT_SECRET` | ✅ چرخید | توکنِ جعلیِ ادمین با کلیدِ قدیمی **۴۰۱**؛ با کلیدِ تازه **۴۰۳** (امضا معتبر، مجوز نه) |

**سلامتِ سرویس پس از چرخش:** صفحه‌ی اصلی ۲۰۰، `/api/ads/placements` ۲۰۰ با محتوای واقعی (۸ محصول، ۸ باشگاه)، `/api/clubs` ۲۰۰، گاردِ ادمین ۴۰۱، و هر دو کران سالم.

**اثرِ پذیرفته‌شده:** با چرخشِ `JWT_SECRET` همه‌ی نشست‌ها باطل شدند و کاربران یک‌بار خارج شدند (رفتارِ موردِ انتظار).

**تصمیمِ مالک درباره‌ی `SMS_API_KEY` و `VAPID_PRIVATE_KEY`:** این دو هرگز در مخزن یا تاریخچه‌ی گیت نبودند و فقط یک‌بار در گفت‌وگوی خصوصیِ توسعه ذکر شدند. مالک تشخیص داد **فعلاً چرخش لازم نیست**. اگر روزی نشانه‌ای از سوءاستفاده دیده شد یا پیش از انتشارِ عمومیِ سایت، در فاز ۷ بازبینی شوند.

**پاک‌سازی‌های کد (انجام‌شده):**
* حذفِ دو fallbackِ هاردکد — `lib/otp-server.ts` و `lib/payments/mock.ts` دیگر با رشته‌ی ثابتِ داخلِ مخزن کار نمی‌کنند و نبودِ `JWT_SECRET` را صریحاً خطا می‌دهند.
* `.gitignore` ریشه به الگوی عامِ `.env*` تغییر کرد تا فایل‌های envِ آینده (`.env.production`، `.env.staging`، …) هم خودبه‌خود بیرون بمانند.

---

# SECURITY CREDENTIAL ROTATION REQUIRED (گزارشِ اولیه — بایگانی)

> ممیزیِ اعتبارنامه‌ها — ۱۴۰۵/۰۵/۰۷ (2026-07-29). **فقط بررسی و مستندسازی؛ هیچ کلیدی چرخانده نشد، هیچ رمز/نشستی تغییر نکرد، هیچ مایگریشن یا تغییرِ کدی انجام نشد.**
>
> **زمینه‌ی حیاتی:** مخزنِ `BabakNabavi/billiardhub` روی گیت‌هاب **عمومی** است و همه‌ی کامیت‌های زیر روی `origin/main` هستند. برخلاف تصورِ اولیه، افشا فقط مربوط به `HANDOFF.md` در فاز ۴ نبود؛ **از ۱۴۰۵/۰۴/۰۲ (2026-06-23) در تاریخچه وجود داشته است.**

## خلاصه‌ی وضعیت

| # | اعتبارنامه | محلِ افشا | در تاریخچه‌ی گیت؟ | در HEAD؟ | هنوز معتبر؟ | چرخش لازم؟ |
|---|---|---|---|---|---|---|
| ۱ | **Supabase `service_role` key** | `PROJECT-HANDOFF-V5.md` | ✅ بله | ❌ خیر | ✅ **آری — آزموده شد** | 🔴 **بحرانی — فوری** |
| ۲ | **`JWT_SECRET`** | سورس‌کد + دو سندِ هندآف | ✅ بله | ❌ خیر | ✅ **آری — همان مقدارِ امروز** | 🔴 **بحرانی — فوری** |
| ۳ | **رمزِ دیتابیس** (داخل `DATABASE_URL`) | `PROJECT-HANDOFF-V5.md`، `HANDOFF.md` | ✅ بله | ❌ خیر | ✅ **آری — همان مقدارِ امروز** | 🟠 **بالا** |
| ۴ | Supabase `anon` key | `PROJECT-HANDOFF-V5.md` | ✅ بله | ❌ خیر | ✅ آری | ⚪ ذاتاً عمومی — به‌خودیِ‌خود لازم نیست |
| ۵ | `VAPID_PRIVATE_KEY` | — | ❌ خیر | ❌ خیر | — | ✅ لازم نیست |
| ۶ | `SMS_API_KEY` | — | ❌ خیر | ❌ خیر | — | ✅ لازم نیست |
| ۷ | `CRON_SECRET` | فقط نامِ متغیر در متن/کد | ❌ خیر | ❌ خیر | — | ✅ لازم نیست |
| ۸ | `ZARINPAL_MERCHANT_ID` | فقط placeholderِ `xxx` | ❌ خیر | ❌ خیر | — | ✅ لازم نیست |

## جزئیاتِ هر مورد

### ۱) Supabase `service_role` key — 🔴 بحرانی‌ترین

* **محلِ افشا:** `PROJECT-HANDOFF-V5.md` — افزوده در `1b6815d` (2026-06-27)، حذف‌شده در `a3fe9a2` (2026-06-28). فایل از HEAD رفته ولی محتوایش در تاریخچه‌ی عمومی می‌ماند.
* **تاریخچه یا فقط Working Tree؟** در **تاریخچه‌ی گیت** و روی `origin/main`.
* **هنوز معتبر است؟** **بله.** آزمونِ فقط‌خواندنی روی `GET /rest/v1/users` با همان کلید **HTTP 200** برگرداند. یعنی این کلید همین حالا دسترسیِ **کاملِ خواندن و نوشتن به همه‌ی جدول‌ها** می‌دهد و **RLS را دور می‌زند** — همان لایه‌ای که در مایگریشن ۰۱۷ اضافه شد. انقضای توکن سالِ ۲۰۳۶ است.
* **اثرِ چرخش روی Production:** کلیدِ تازه باید در `SUPABASE_SERVICE_ROLE_KEY` در Vercel و `.env.local` بنشیند. تا لحظه‌ی به‌روزرسانیِ env، همه‌ی API routeهای سرورساید (کلِ بک‌اند) کار نمی‌کنند ⇒ چند دقیقه قطعیِ کوتاه.
* **مراحل:** داشبورد Supabase ← Settings ← API ← بخشِ کلیدها ← چرخاندن/ساختنِ کلیدِ تازه و **باطل‌کردنِ کلیدِ قدیمی** ← به‌روزرسانیِ متغیر در Vercel (Production و Preview) ← Redeploy ← به‌روزرسانیِ `.env.local` محلی.

### ۲) `JWT_SECRET` — 🔴 بحرانی

* **محلِ افشا (سه جا):**
  * `apps/web/app/api/products/route.ts` و `apps/web/app/booking/my/route.ts` — کامیت `1aac207` (2026-06-23)، به‌شکلِ **fallbackِ هاردکد در سورس**: `process.env.JWT_SECRET || '…'`.
  * `PROJECT-HANDOFF-V5.md` — کامیت `1b6815d`.
  * `HANDOFF.md` — کامیت `33b4b32` (فاز ۴)، پاک‌شده در `947c15b`.
* **تاریخچه یا فقط Working Tree؟** در **تاریخچه‌ی گیت** و روی `origin/main`.
* **هنوز معتبر است؟** **بله — مقدارِ فعالِ امروز دقیقاً همان مقدارِ لو رفته است** (با مقایسه‌ی `apps/web/.env.local` تأیید شد).
* **اثر:** با این کلید می‌توان توکنِ نشستِ **هر کاربری، از جمله ادمین** را جعل کرد؛ یعنی دور زدنِ کاملِ احراز هویت و همه‌ی گاردهای ادمین.
* **اثرِ چرخش روی Production:** همه‌ی نشست‌های فعلی **باطل می‌شوند** و کاربران باید دوباره وارد شوند (توکن‌های `bh_at`/`bh_rt` با کلیدِ قدیمی امضا شده‌اند). این تنها اثرِ کاربرمحورِ چرخش است.
* **مراحل:** ساختِ رشته‌ی تصادفیِ بلند (مثلاً ۶۴ بایتِ hex) ← جای‌گذاری در `JWT_SECRET` در Vercel و `.env.local` ← Redeploy. **نکته:** `NID_HMAC_SECRET` تنظیم نشده و `lib/identity.ts` برای هشِ کد ملی از نمکِ داخلِ دیتابیس استفاده می‌کند، پس **چرخاندنِ `JWT_SECRET` هویت‌های فاز ۳ را یتیم نمی‌کند**.

### ۳) رمزِ دیتابیس (داخلِ `DATABASE_URL`) — 🟠 بالا

* **محلِ افشا:** `PROJECT-HANDOFF-V5.md` (`1b6815d`) و `HANDOFF.md` (`33b4b32`، پاک‌شده در `947c15b`).
* **تاریخچه یا فقط Working Tree؟** در **تاریخچه‌ی گیت** و روی `origin/main`.
* **هنوز معتبر است؟** **بله — رمزِ فعالِ امروز همان رمزِ لو رفته است.**
* **اثر:** اتصالِ مستقیم به پستگرس با کاربرِ `postgres` از طریقِ pooler؛ خواندن/نوشتن/حذفِ کامل، مستقل از اپلیکیشن.
* **اثرِ چرخش روی Production:** تا وقتی `DATABASE_URL` تازه در Vercel ننشیند، هر چیزی که مستقیم به دیتابیس وصل می‌شود قطع است. (بیشترِ مسیرهای اپ از PostgREST استفاده می‌کنند نه اتصالِ مستقیم، ولی اسکریپت‌ها/مایگریشن‌ها از این رشته استفاده می‌کنند.)
* **مراحل:** داشبورد Supabase ← Settings ← Database ← Reset database password ← به‌روزرسانیِ `DATABASE_URL` در Vercel و `.env` ریشه.

### ۴) Supabase `anon` key — ⚪ بدونِ نیاز به چرخشِ مستقل

این کلید **طراحی‌شده برای عمومی‌بودن** است و در باندلِ مرورگر به هر بازدیدکننده تحویل می‌شود؛ بودنش در تاریخچه چیزِ تازه‌ای لو نمی‌دهد. تنها دفاعِ واقعی در برابرش RLS است که در مایگریشن ۰۱۷ برقرار شد. **نکته‌ی عملی:** اگر کلیدهای پروژه در Supabase چرخانده شوند، این کلید هم عوض می‌شود و باید در `NEXT_PUBLIC_SUPABASE_ANON_KEY` (هم Vercel، هم `.env.local`، هم `apps/web/.env.production`) به‌روز شود.

## وضعیت فایل‌های محیطی و `.gitignore`

* **هیچ فایلِ `.env`ای در مخزن ترک نشده است** — نه حالا، نه در هیچ کامیتی از تاریخچه (بررسیِ `--diff-filter=A` روی کلِ تاریخچه).
* فایل‌های روی دیسک و وضعیتشان:
  * `.env` (ریشه) → ignore می‌شود (قاعده‌ی `.gitignore:9`)
  * `apps/web/.env.local` → ignore می‌شود (`apps/web/.gitignore:29` = `.env*`)
  * `apps/web/.env.production` → ignore می‌شود (همان قاعده)
* **شکافِ باقی‌مانده (پیشنهاد، اعمال نشد):** `.gitignore` ریشه به‌جای الگوی عام، نام‌های مشخص را فهرست کرده (`.env`, `.env.local`, `.env.*.local`). بنابراین اگر روزی فایلی مثل `/.env.production` یا `/.env.staging` **در ریشه** ساخته شود، **ignore نمی‌شود**. افزودنِ `.env*` به `.gitignore` ریشه این حفره را می‌بندد. طبق دستور شما تغییرش ندادم.

## وضعیت HEAD

فایل‌های ترک‌شده‌ی فعلی **هیچ مقدارِ واقعی‌ای ندارند**. تنها هم‌خوانیِ الگو، یک نمونه‌ی ساختگی در `PROJECT_ARCHITECTURE.md` است (`postgresql://postgres:password@db.xxx.supabase.co`) که placeholder است، نه اعتبارنامه‌ی واقعی.

## جمع‌بندی و اولویت

۱. **همین حالا:** چرخاندنِ کلیدِ `service_role` (تنها موردی که آزمونِ زنده ثابت کرد همین لحظه دسترسیِ کاملِ دیتابیس می‌دهد).
۲. **همین حالا:** چرخاندنِ `JWT_SECRET` (جعلِ نشستِ ادمین) — با پذیرشِ خروجِ اجباریِ کاربران.
۳. **در اولین فرصت:** تغییرِ رمزِ دیتابیس.
۴. **اختیاری ولی توصیه‌شده:** چون این مقادیر ماه‌ها روی مخزنِ عمومی بوده‌اند، پاک‌سازیِ تاریخچه (مثلاً `git filter-repo`) جلوی خواندنِ آینده را می‌گیرد، ولی **جایگزینِ چرخش نیست** — باید فرض کرد لو رفته‌اند. این کار بازنویسیِ تاریخچه و force-push می‌خواهد و بدونِ دستور صریحِ شما انجام نمی‌شود.
۵. پس از چرخش، بازبینیِ نهاییِ امنیتی در **PHASE 7** (طبق تصمیم D3) شاملِ راستی‌آزماییِ باطل‌شدنِ کلیدهای قدیمی باشد.

---

# PRE-ROTATION DEPENDENCY MAP & POST-ROTATION VERIFICATION

> آماده‌سازیِ چرخشِ دستیِ اعتبارنامه‌ها — ۱۴۰۵/۰۵/۰۷ (2026-07-29).
> **هیچ کلیدی چرخانده نشد، هیچ کلیدِ تازه‌ای ساخته یا جای‌گزین نشد، هیچ مقدارِ واقعی در هیچ فایلی نوشته نشد، هیچ تغییرِ کد یا دیتابیسی انجام نشد.** فقط نقشه‌ی وابستگی و چک‌لیستِ راستی‌آزمایی.

## ۱) محلِ خوانده‌شدنِ هر اعتبارنامه از Environment

### `JWT_SECRET`

| فایل | خط | کاربرد |
|---|---|---|
| `apps/web/lib/auth/session.ts` | ۴۴–۴۵ | امضا/تأییدِ توکنِ دسترسی (HS256، نودی) — اگر تنظیم نباشد صریحاً خطا می‌دهد |
| `apps/web/lib/auth/edge.ts` | ۲۳، ۲۹ | تأییدِ توکن در middleware (jose، رانتایمِ Edge) |
| `apps/web/lib/auth/store.ts` | ۲۰ | **هشِ HMACِ رفرش‌توکن** که در جدولِ `sessions` ذخیره می‌شود |
| `apps/web/lib/auth/store.ts` | ۱۱۰ | ساختِ توکنِ CSRF (فقط به‌عنوان منبعِ تصادفی) |
| `apps/web/app/api/auth/login/route.ts` | ۵۵ | صدورِ توکن هنگامِ ورود |
| `apps/web/app/api/auth/register/route.ts` | ۹۳ | صدورِ توکن هنگامِ ثبت‌نام |
| `apps/web/lib/otp-server.ts` | ۶ | **هشِ کدِ OTP** — با fallbackِ هاردکد |
| `apps/web/lib/payments/mock.ts` | ۱۲ | امضای درگاهِ ساختگی — با fallbackِ هاردکد |
| `apps/web/lib/identity.ts` | ۵۹ | فقط **آخرین‌راه‌حل** اگر نمکِ داخلِ دیتابیس در دسترس نباشد |
| `apps/api/src/modules/auth/auth.module.ts` | ۲۲ | بک‌اندِ NestJS — **دیپلوی نمی‌شود** |
| `apps/api/src/modules/auth/jwt.strategy.ts` | ۱۲ | همان — **دیپلوی نمی‌شود** |
| `turbo.json` | ۱۱ | اعلانِ متغیر در passthroughِ Turborepo |

### `SUPABASE_SERVICE_ROLE_KEY`

| فایل | خط | کاربرد |
|---|---|---|
| `apps/web/lib/supabase-server.ts` | ۱۰ | **تنها سازنده‌ی کلاینتِ سرورساید** (singleton). نبودنش ⇒ پرتابِ خطا |
| `apps/web/lib/social-server.ts` | ۶۱ | کلاینتِ جدا برای بخشِ اجتماعی/استوری |
| `turbo.json` | ۱۰ | اعلانِ متغیر |

مصرف‌کنندگانِ غیرمستقیم: ۲۸ فایل. پنج ماژولِ زیرساختی (`lib/auth/store.ts`، `lib/finance/db.ts`، `lib/identity.ts`، `lib/profiles/server.ts`، `lib/social-server.ts`) و مسیرهای API در دسته‌های `clubs` (۷)، `auth` (۷)، `users` (۲)، `sellers` (۲)، `products` (۲)، `otp` (۱)، `live` (۱) — و از طریقِ `finance/db.ts` عملاً **کلِ سیستمِ تبلیغات، سهمیه، پرداخت و پنلِ ادمین**.

### `DATABASE_URL` (رمزِ دیتابیس)

| فایل | خط | کاربرد |
|---|---|---|
| `apps/api/data-source.ts` | ۱۰ | TypeORM — بک‌اندِ NestJS، **دیپلوی نمی‌شود** |
| `apps/api/src/app.module.ts` | ۱۹ | همان |
| `.env` ریشه (خارج از مخزن) | — | اسکریپت‌های محلی و اجرای مایگریشن‌ها |

**نکته‌ی کلیدی:** اپلیکیشنِ وبِ روی پروداکشن **هیچ‌جا `DATABASE_URL` را نمی‌خواند**؛ همه‌ی دسترسی‌اش از راهِ PostgREST با کلیدِ `service_role` است.

## ۲) fallbackهای هاردکدِ مربوط به Secret

**دو موردِ اصلی که گزارش شده بودند، همین حالا در HEAD وجود ندارند:**

* `apps/web/app/api/products/route.ts` — الگوی `process.env.JWT_SECRET || '<مقدارِ واقعی>'` **حذف شده**؛ فایل اکنون از `sessionFromRequest` استفاده می‌کند.
* `apps/web/app/booking/my/route.ts` — **فایل کلاً حذف شده است.**

هر دو فقط در **تاریخچه‌ی گیت** باقی مانده‌اند (بخشِ بالا).

**fallbackهای باقی‌مانده در HEAD** (مقدارشان اسرارِ واقعیِ لو رفته نیست، ولی الگو خطرناک است چون نبودِ env را بی‌صدا می‌پوشاند):

| فایل | خط | الگو | خطر |
|---|---|---|---|
| `apps/web/lib/otp-server.ts` | ۶ | `process.env.JWT_SECRET \|\| 'bh-otp-secret'` | اگر env نباشد، کدهای OTP با کلیدِ ثابتِ **قابلِ‌خواندن در مخزنِ عمومی** هش می‌شوند |
| `apps/web/lib/payments/mock.ts` | ۱۲ | `process.env.JWT_SECRET \|\| 'bh-mock-secret'` | فقط درگاهِ ساختگی؛ خطرِ عملی کم |

هیچ fallbackِ هاردکدی برای `SUPABASE_SERVICE_ROLE_KEY` یا `DATABASE_URL` وجود ندارد (اولی صریحاً خطا می‌دهد). بقیه‌ی fallbackهای یافت‌شده در پروژه اسرار نیستند (`NEXT_PUBLIC_API_URL`، `PAYMENT_PROVIDER`، `VERCEL_GIT_COMMIT_SHA`، …).

## ۳) نیاز به Redeploy پس از هر چرخش

| اعتبارنامه | Redeploy لازم؟ | چرا |
|---|---|---|
| `SUPABASE_SERVICE_ROLE_KEY` | ✅ **بله، اجباری** | کلاینت به‌شکلِ singleton در حافظه‌ی instance کش می‌شود؛ تا instanceها بازسازی نشوند کلیدِ قدیمی در حافظه می‌ماند |
| `JWT_SECRET` | ✅ **بله، اجباری** | در `edge.ts` داخلِ متغیرِ ماژولی کش می‌شود (`cached ??=`) |
| `DATABASE_URL` | ❌ خیر | اپِ وب اصلاً نمی‌خواندش؛ فقط ابزارِ محلی |

در Vercel: تغییرِ متغیرِ محیطی **به‌خودیِ‌خود اعمال نمی‌شود** — پس از ذخیره باید Redeploy زده شود (یا از «Redeploy» روی آخرین دیپلویمنت استفاده شود). هر دو محیطِ **Production** و **Preview** باید به‌روز شوند.

## ۴) متغیرهایی که در Vercel باید به‌روز شوند

| متغیر | محیط | توضیح |
|---|---|---|
| `SUPABASE_SERVICE_ROLE_KEY` | Production + Preview | کلیدِ تازه‌ی سرورساید |
| `JWT_SECRET` | Production + Preview | رشته‌ی تصادفیِ بلندِ تازه |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Production + Preview | **فقط اگر** چرخشِ Supabase کلیدِ anon را هم عوض کند (معمولاً می‌کند) |
| `DATABASE_URL` | — | در Vercel لازم نیست؛ ولی اگر تعریف شده، به‌روز شود |

فایل‌های محلی (خارج از مخزن): `.env` ریشه (`DATABASE_URL`) و `apps/web/.env.local` (`JWT_SECRET`، `SUPABASE_SERVICE_ROLE_KEY`، `NEXT_PUBLIC_SUPABASE_ANON_KEY`) و `apps/web/.env.production` (`NEXT_PUBLIC_SUPABASE_ANON_KEY`).

## ۵) اثرِ چرخشِ `JWT_SECRET` روی احراز هویت

| جزء | اثر |
|---|---|
| **توکنِ دسترسی** (`bh_at`) | ❌ همه باطل می‌شوند (HS256 با کلیدِ قدیمی) |
| **رفرش‌توکن** (`bh_rt`) | ❌ باطل — چون در جدولِ `sessions` فقط **هشِ HMACِ کلیددار** ذخیره شده؛ با کلیدِ تازه هیچ هشی تطبیق نمی‌کند |
| **جدولِ `sessions`** | ردیف‌های قدیمی می‌مانند ولی هرگز تطبیق نمی‌کنند (زباله‌ی بی‌ضرر) |
| **کوکیِ CSRF** (`bh_csrf`) | ✅ سالم — اعتبارسنجی «double-submit» است (مقایسه‌ی کوکی با هدرِ `x-csrf-token`)، نه تأییدِ امضا |
| **کدهای OTP در جریان** | ❌ کدهای صادرشده‌ی قبل از چرخش تأیید نمی‌شوند؛ کاربر باید کدِ تازه بگیرد |
| **هویت/سهمیه‌ی فاز ۳** | ✅ **بی‌اثر** — هشِ کد ملی با نمکِ داخلِ `app_settings` ساخته می‌شود نه با `JWT_SECRET` |
| **درگاهِ ساختگیِ پرداخت** | فقط در dev؛ تراکنش‌های ناتمامِ mock بی‌اعتبار می‌شوند |

**خلاصه:** همه‌ی کاربران یک‌بار خارج می‌شوند و باید دوباره وارد شوند. هیچ داده‌ای از دست نمی‌رود.

## ۶) اثرِ چرخشِ رمزِ دیتابیس

| مسیر | اثر |
|---|---|
| **اپِ وبِ پروداکشن** | ✅ **هیچ** — از PostgREST استفاده می‌کند، نه اتصالِ مستقیم |
| **Connection Pooler** (`…pooler.supabase.com:6543`) | رشته‌ی اتصال باید با رمزِ تازه به‌روز شود |
| **اتصالِ مستقیم** (`db.<ref>.supabase.co:5432`) | همان |
| **کران‌های Vercel** | ✅ بی‌اثر — از مسیرهای HTTP اپ می‌روند و با `CRON_SECRET` محافظت می‌شوند |
| **`apps/api` (NestJS)** | نظری؛ **دیپلوی نشده** (`vercel.json` ندارد) |
| **اسکریپت‌های محلی و مایگریشن‌ها** | ❌ تا به‌روزرسانیِ `.env` ریشه کار نمی‌کنند |

## ۷) اثرِ چرخشِ `service_role`

| جزء | اثر |
|---|---|
| `lib/supabase-server.ts` | تنها نقطه‌ی ساخت؛ **singleton در حافظه** ⇒ حتماً Redeploy |
| `lib/social-server.ts` | کلاینتِ جدا برای استوری/اجتماعی |
| ۲۸ فایلِ مصرف‌کننده | شاملِ کلِ `auth`، `clubs`، `users`، `sellers`، `products`، `otp`، `live` |
| از راهِ `lib/finance/db.ts` | کلِ **تبلیغات، سهمیه، پرداخت، پنلِ ادمین** |
| **کران‌ها** | `expire-campaigns` و `expire-bookings` هر دو از همین کلاینت استفاده می‌کنند |
| **پنجره‌ی قطعی** | بینِ باطل‌شدنِ کلیدِ قدیمی و اتمامِ Redeploy، عملاً **همه‌ی مسیرهای سرورساید ۵۰۰/۴۰۱** می‌دهند |

**توصیه‌ی ترتیب:** اول کلیدِ تازه ساخته و در Vercel ذخیره شود و Redeploy کامل شود، **سپس** کلیدِ قدیمی باطل گردد — این کار پنجره‌ی قطعی را تقریباً صفر می‌کند. (اگر سرویس اجازه‌ی هم‌زیستیِ دو کلید را ندهد، قطعیِ کوتاه اجتناب‌ناپذیر است.)

## ۸) چک‌لیستِ راستی‌آزماییِ پس از چرخش

**الف) کلیدِ قدیمی واقعاً باطل شده؟**
- [ ] فراخوانیِ `GET /rest/v1/users?select=id&limit=1` با کلیدِ **قدیمیِ** `service_role` باید **۴۰۱** بدهد (امروز ۲۰۰ می‌دهد).
- [ ] ساختِ یک توکنِ HS256 با `JWT_SECRET` **قدیمی** و فرستادنش به یک مسیرِ محافظت‌شده باید **۴۰۱** بگیرد.
- [ ] اتصالِ `psql` با رمزِ **قدیمی** باید رد شود.

**ب) سرویس سالم است؟**
- [ ] `GET /api/version` پاسخِ ۲۰۰ با SHA دیپلویِ تازه.
- [ ] `GET /api/ads/placements` ⇒ ۲۰۰ و سه جایگاهِ رایگان با محتوای واقعی (کلیدِ تازه‌ی service_role کار می‌کند).
- [ ] صفحه‌ی اصلی ۲۰۰ و سکشن‌های محصولات/باشگاه‌ها پر.
- [ ] `GET /api/admin/advertising` بدونِ احراز ⇒ ۴۰۱ (گارد سالم).

**ج) احراز هویت پس از چرخش**
- [ ] ورودِ تازه با یک حساب واقعی ⇒ کوکی‌های `bh_at`/`bh_rt` ست می‌شوند.
- [ ] رفرشِ نشست کار می‌کند (نشستِ تازه در جدولِ `sessions`).
- [ ] نشستِ قدیمی (مرورگرِ باز از قبل) به صفحه‌ی ورود هدایت می‌شود — **این رفتارِ درست است**.
- [ ] ارسالِ OTP و تأییدش با کدِ تازه کار می‌کند.

**د) کارهای زمان‌بندی‌شده**
- [ ] فراخوانیِ دستیِ `/api/cron/expire-campaigns` ⇒ `{ok:true}`.
- [ ] فراخوانیِ دستیِ `/api/cron/expire-bookings` ⇒ پاسخِ سالم.

**ه) پاک‌سازیِ بعدی (اختیاری)**
- [ ] حذفِ دو fallbackِ هاردکدِ باقی‌مانده (`otp-server.ts`، `payments/mock.ts`) تا نبودِ env به‌جای پوشاندهشدن، صدا کند.
- [ ] افزودنِ `.env*` به `.gitignore` ریشه.
- [ ] تصمیم درباره‌ی پاک‌سازیِ تاریخچه‌ی گیت (جایگزینِ چرخش نیست).
- [ ] خالی‌کردنِ ردیف‌های بی‌مصرفِ جدولِ `sessions` (اختیاری، صرفاً نظافت).

---

## Current Phase

PHASE 6 — PAYMENT, AD BOOKING & ADVERTISER DASHBOARD

## Status

COMPLETED — ۱۴۰۵/۰۵/۰۷ (2026-07-29)

---

## Payment (فاز ۶)

از قراردادِ موجودِ `lib/payments` استفاده شد (mock | zarinpal) — هیچ درگاهِ تازه‌ای اضافه نشد و منطقِ رزرو به درگاهِ خاصی وابسته نیست.

**چرخه‌ی کامل:** انتخابِ جایگاه → انتخابِ پلن (که مدت را تعیین می‌کند) → **محاسبه‌ی قیمت در سرور** → ساختِ سفارش → درگاه → **verify سرورساید** → `PENDING_REVIEW` → تأییدِ ادمین → `SCHEDULED`/`ACTIVE` → `EXPIRED` (خودکار).

## Orders (فاز ۶)

`campaign_orders` که در فاز ۲ فقط اسکیما بود، حالا زنده است:

* وضعیت‌های پرداخت: `PENDING` · `PAID` · `FAILED` · `CANCELED` · **`REFUNDED`** (تازه اضافه شد).
* `provider` و `provider_authority` (شناسه‌ی درگاه) و `provider_ref_id` (**شماره‌ی پیگیریِ تراکنش**) ذخیره می‌شوند.
* ستونِ تازه‌ی `duration_days`: مدتِ خریداری‌شده روی خودِ سفارش **قفل** می‌شود تا تغییرِ بعدیِ پلن، چیزی را که کاربر پول داده عوض نکند.
* `snapshot` نامِ جایگاه/پلن و قیمتِ لحظه‌ی خرید را نگه می‌دارد.

## Verification & Security (فاز ۶)

* **قیمت هرگز از کلاینت خوانده نمی‌شود.** کلاینت فقط `placementKey` و `planId` می‌فرستد؛ مبلغ از ردیفِ پلن در دیتابیس درمی‌آید. تست: فرستادنِ `amount:1` هیچ اثری ندارد.
* **پلن باید به همان جایگاه تعلق داشته باشد** — وگرنه می‌شد پلنِ ارزانِ یک جایگاه را به جایگاهِ گران چسباند (۴۰۹).
* **verify فقط سرورساید**؛ مبلغِ واقعیِ پرداخت‌شده با مبلغِ سفارش مقایسه می‌شود و ناهم‌خوانی، سفارش را `FAILED` می‌کند و در `audit` ثبت می‌شود.
* **فعال‌سازی اتمیک و ایدمپوتنت** (`bh_activate_campaign` با `FOR UPDATE`): کالبکِ تکراری هیچ اثرِ دومی ندارد.
* **گاردِ ظرفیت در همان تراکنش**: دو خریدِ هم‌زمان برای آخرین جای خالی، هر دو موفق نمی‌شوند. نکته‌ی مهمِ پیاده‌سازی: این حالت عمداً `RAISE EXCEPTION` نمی‌کند، چون استثنا کلِ تراکنش را برمی‌گرداند و علامت‌گذاریِ سفارش به‌عنوان `FAILED` هم خنثی می‌شد و سفارش برای همیشه `PENDING` می‌ماند.
* **کالبک‌ها به `CSRF_EXEMPT` اضافه شدند** (`ads/campaigns`، `ads/plans`، `stories/plans`) — کالبکِ درگاه کوکیِ CSRF ما را ندارد؛ امنیت از راهِ verifyِ سرورساید تأمین می‌شود. این همان تله‌ای بود که ممیزیِ فاز ۱ علامت زده بود.

## Eligibility (فاز ۶)

کاربر فقط جایگاه‌هایی را می‌بیند که **نقشِ تأییدشده‌اش** (فاز ۳) اجازه می‌دهد — نه نقشِ خوداظهار که خودِ کاربر می‌تواند عوضش کند:

| جایگاه | نقشِ لازم |
|---|---|
| محصولات ویژه‌ی بازار | تولیدکننده · فروشنده |
| باشگاه‌های پیشنهادی | باشگاه‌دار |
| فروشگاه‌های تجهیزات | فروشنده |
| سه بنرِ عمومی | هر کسب‌وکارِ تأییدشده |

کاربرِ بدونِ هویتِ تأییدشده پاسخِ `identityRequired` می‌گیرد و هیچ جایگاهی نمی‌بیند. جایگاهی که پله‌ی قیمتی ندارد هم پیشنهاد نمی‌شود.

## Campaign Lifecycle (فاز ۶)

`PENDING_PAYMENT` (لحظه‌ی ساخت — هرگز نمایش داده نمی‌شود) → `PENDING_REVIEW` (پس از پرداختِ تأییدشده) → `ACTIVE`/`SCHEDULED` (تأییدِ ادمین) → `EXPIRED` (کران).

**پنجره‌ی زمانی از لحظه‌ی تأییدِ ادمین شروع می‌شود، نه از لحظه‌ی پرداخت** (`bh_approve_campaign`) — وگرنه تأخیرِ بررسی از مدتی که تبلیغ‌دهنده پول داده کم می‌کرد.

## Advertiser Dashboard (فاز ۶)

`/advertise/dashboard` — کمپین‌های خودِ کاربر با فیلترِ وضعیت (همه/فعال/در انتظار بررسی/در انتظار پرداخت/زمان‌بندی‌شده/منقضی/رد‌شده/لغو‌شده) و برای هر کمپین: **جایگاه، پلن، مبلغ، وضعیتِ پرداخت، تاریخ شروع، تاریخ پایان، وضعیت، نمایش، کلیک، CTR**. بالای صفحه هم جمعِ کمپین‌های فعال، نمایش، کلیک و هزینه‌ی پرداخت‌شده. کاربر فقط کمپین‌های خودش را می‌بیند (تست شد).

## Analytics (فاز ۶)

آمارِ هر کمپین همان شمارنده‌های فاز ۴ است (`impressions`/`clicks` با CTR محاسبه‌شده) و در داشبوردِ تبلیغ‌دهنده و پنلِ ادمین یک عدد را نشان می‌دهند.

## Database Changes (فاز ۶)

مایگریشن **۰۲۱**: افزودنِ `REFUNDED` به قیدِ وضعیتِ سفارش، ستونِ `duration_days`، ایندکسِ `(user_id, created_at DESC)`، و دو تابعِ اتمیکِ `bh_activate_campaign` و `bh_approve_campaign` — هر دو با `REVOKE ... FROM PUBLIC, anon, authenticated`.

## Tests (فاز ۶)

**۴۱/۴۱ پاس**: گاردِ ورود؛ **واجدِ شرایط بودن** (فروشنده فروشگاه‌ها را می‌بیند ولی باشگاه‌ها را نه، و برعکس؛ کاربرِ عادی هیچ؛ بدونِ هویت هیچ)؛ **امنیتِ قیمت** (قیمتِ ارسالیِ کلاینت نادیده گرفته شد، پلنِ جایگاهِ دیگر رد شد، جایگاهِ خارج از نقش ۴۰۳)؛ **چرخه‌ی کامل** (شروع در `PENDING_PAYMENT`، عدمِ نمایش تا پرداخت، کالبک، ثبتِ شماره‌ی پیگیری، انتقال به `PENDING_REVIEW`، **ایدمپوتنسیِ کالبکِ تکراری**، عدمِ نمایش تا تأیید، تأییدِ ادمین، **پنجره برابرِ مدتِ خریداری‌شده و از لحظه‌ی تأیید**، نمایش پس از تأیید، انقضای خودکار)؛ **پرداختِ لغوشده** (سفارش و کمپین هر دو لغو)؛ **گاردِ ظرفیت**؛ و **داشبورد** (کاملیِ فیلدها، جمع‌ها، و اینکه کاربرِ دیگر هیچ‌کدام را نمی‌بیند).

## Known Issues (فاز ۶)

* **اختلافِ ساعتِ اپ و دیتابیس**: زمان‌ها را دیتابیس می‌نویسد ولی فیلترِ نمایش با ساعتِ سرورِ اپ اجرا می‌شود. در همین محیط ۱۴ ثانیه اختلاف دیده شد که یعنی کمپینِ تازه‌تأییدشده چند ثانیه نامرئی می‌ماند. با مهلتِ دو دقیقه‌ای در فیلترِ نمایش بسته شد (هزینه‌اش حداکثر دو دقیقه زودتر شروع‌شدنِ کمپینِ زمان‌بندی‌شده است).
* بازگشتِ وجه (`REFUNDED`) در اسکیما و پنل پشتیبانی می‌شود ولی **فلوی خودکارِ refund** پیاده نشده؛ فعلاً دستی است (همان محدودیتِ زرین‌پال که در ممیزیِ فاز ۱ ثبت شد).
* خریدِ تبلیغ در `campaign_orders` ثبت می‌شود ولی هنوز ردیفی در `ledger_entries` نمی‌سازد، پس در داشبوردِ مالیِ ادمین دیده نمی‌شود — همان نقصی که برای بسته‌های آگهی هم وجود دارد.
* `ad_credits` همچنان استفاده نشده؛ خرید مستقیماً یک کمپین می‌سازد، نه اعتبارِ قابلِ‌مصرف.

## Next Phase

PHASE 7 — (منتظر دستور شما) · طبق تصمیم D3، **Security Audit نهایی** پیش از عمومی‌شدنِ سایت.

---

# PHASE 5 — CONNECT ADVERTISING SYSTEM TO HOMEPAGE (آرشیو)

COMPLETED — ۱۴۰۵/۰۵/۰۷ (2026-07-29)

---

## Homepage Integrations (فاز ۵)

صفحه‌ی اصلی از این پس محتوایش را از سیستمِ جایگاه‌ها می‌گیرد. ظاهر و چیدمان دست‌نخورده ماند؛ فقط منبعِ داده و قواعدِ نمایش عوض شد.

| # | سکشن | جایگاه | حالتِ فعلی | محتوا |
|---|---|---|---|---|
| ۱ | بیلیارد بازار | `market_featured_products_homepage` | رایگان · ظرفیت ۱۴ | تازه‌ترین محصولاتِ فعالِ بازار |
| ۲ | باشگاه‌های پیشنهادی | `featured_clubs_homepage` | رایگان · ظرفیت ۸ | تازه‌ترین باشگاه‌های فعال، تأییدشده‌ها اول |
| ۳ | فروشگاه‌های تجهیزات | `featured_equipment_stores_homepage` | رایگان · ظرفیت ۱۲ | تازه‌ترین فروشگاه‌های تأییدشده |
| ۴ | تبلیغ سمت راست | `equipment_ads_right` | پولی · خاموش | کمپینِ بنری (اسلایدر) |
| ۵ | تبلیغ سمت چپ | `equipment_ads_left` | پولی · خاموش | کمپینِ بنری (اسلایدر) |
| ۶ | بنر پایینِ صفحه | `homepage_bottom_banner` | پولی · خاموش | بنرِ عریضِ بالای فوتر |

**قواعدِ نمایش** (هر شش جایگاه، بدونِ استثنا):

* **غیرفعال ⇒ اصلاً رندر نمی‌شود.** برای سه سکشنِ ویژه یعنی کلِ سکشن از صفحه حذف می‌شود، نه اینکه قابِ خالی بماند.
* **رایگان ⇒** محتوای پیش‌فرض از داده‌ی واقعیِ سایت (لایه‌ی تازه‌ی `lib/ads/free.ts`).
* **دستی ⇒** فقط کمپین‌هایی که ادمین ساخته.
* **پولی ⇒** فقط کمپین‌های `ACTIVE` داخلِ پنجره‌ی زمانی.
* تغییرِ حالت از پنلِ ادمین **هیچ تغییری در کدِ صفحه لازم ندارد** — کلاینت فرقی بینِ محتوای رایگان و کمپینی نمی‌بیند.

**استقلالِ کامل:** خاموش‌کردنِ فهرستِ فروشگاه‌ها بنرهای کناریِ همان سکشن را حذف نمی‌کند و برعکس؛ سکشنِ فروشندگان تا وقتی یکی از این سه چیزی برای نشان‌دادن دارد می‌ماند.

## Components Changed (فاز ۵)

* **جدید** `apps/web/lib/ads/free.ts` — منبعِ محتوای رایگان. خروجی دقیقاً همان `EntitySnapshot` مسیرِ کمپینی است. ترتیب: تازه‌ترین اول، با **کلیدِ شکستِ تساوی** (`id`/`slug`) چون داده‌ی seed همه یک زمانِ ثبت دارند و بدونِ آن ترتیب بینِ درخواست‌ها می‌پرید.
* `app/api/ads/placements/route.ts` — جایگاهِ `free` محتوایش را از `freeContent` می‌گیرد؛ `mode` هم در پاسخ آمد.
* `components/ads/AdSlot.tsx` — هوکِ تازه‌ی `usePlacementState` که «در حالِ بارگذاری / خاموش / روشن» را تفکیک می‌کند (نبودنِ کلید در پاسخ = خاموش).
* `app/page.tsx` — گاردِ نمایشِ سه سکشن، استقلالِ بنرها از فهرستِ فروشگاه‌ها، و انتقالِ `homepage_bottom_banner` به انتهای همین صفحه.
* `app/layout.tsx` — بنرِ پایین **از layout برداشته شد**: آن‌جا روی همه‌ی مسیرها می‌نشست، در حالی که هم کلیدش و هم مشخصاتِ فاز ۵ می‌گویند فقط پایینِ صفحه‌ی اصلی.
* `supabase/migrations/020_homepage_free_placements.sql` — سه جایگاهِ موجودیتی روی `free` + فعال (با مارکرِ `mig_020_data_done`). سه جایگاهِ بنری عمداً پولی و خاموش ماندند تا تا وقتی تبلیغی فروخته نشده چیزی روی صفحه نیاید.

## Mobile Changes (فاز ۵)

هر سه فهرست کاروسلِ افقی‌اند و در موبایل با کشیدنِ انگشت کار می‌کنند (اسکرولِ بومی + `scroll-snap`, بدونِ نوارِ اسکرول):

* باشگاه‌ها — `.clubs-mobile-slider` (کارت ۴۷.۳۶vw، snap مرکزی)
* محصولات — `.mkt-mobile-slider`
* فروشگاه‌ها — `.sellers-mob` (کارت ۵۳vw)

در دسکتاپ همان نوارهای درگ‌شونده‌ی قبلی‌اند (`useHorizontalScroll` با اصلاحِ علامتِ RTL). هر سه سکشن `overflow:hidden` دارند، پس هیچ‌کدام صفحه را افقی عریض نمی‌کنند. بنرِ پایین در قابِ `max-width:1240` وسط‌چین است و در موبایل تمام‌عرض می‌شود.

**اصلاحِ داده‌ی ساختگی:** کارتِ باشگاه پیش‌تر برای هر باشگاهِ واقعی امتیازِ ۵ و «۱۰ میز» جعل می‌کرد. حالا تعدادِ میز از ستون‌های واقعیِ باشگاه جمع می‌شود و اگر باشگاه اعلامش نکرده باشد، آن بخش از کارت **نمایش داده نمی‌شود**؛ امتیاز هم تا وقتی سیستمِ نظر نداریم پنهان است.

## Tests (فاز ۵)

**۳۷/۳۷ پاس** (بیلدِ production محلی + دیتابیسِ زنده، با بازگرداندنِ کاملِ وضعیت):

* **رایگان**: هر سه جایگاه زنده و در حالتِ `free`؛ تعدادِ محصولات = `min(محصولاتِ فعال, ۱۴)`؛ باشگاه‌ها = `min(باشگاه‌های فعال, ۸)`؛ اسنپ‌شات‌ها لینکِ واقعی دارند؛ ترتیبِ باشگاه‌ها تأییدشده‌ها را اول می‌آورد؛ **ترتیب بینِ درخواست‌ها پایدار است** (کارت‌ها نمی‌پرند).
* **غیرفعال ⇒ هیچ**: جایگاهِ خاموش از پاسخِ عمومی حذف می‌شود و بقیه دست‌نخورده می‌مانند؛ روشن‌کردنِ دوباره برش می‌گرداند.
* **استقلال**: خاموش‌کردنِ همه ⇒ هیچ؛ روشن‌کردنِ یکی فقط همان یکی؛ روشن‌کردن‌های بعدی جمع می‌شوند.
* **دستی**: بدونِ کمپین ⇒ خالی (محتوای رایگان دیگر استفاده نمی‌شود)؛ با کمپین ⇒ دقیقاً همان انتخابِ ادمین و نه محتوای رایگان.
* **پولی**: فقط کمپینِ `ACTIVE`؛ تغییرِ وضعیت به «در انتظار بررسی» فوراً از نمایش حذفش می‌کند؛ بازگشت به `free` محتوای سایت را برمی‌گرداند.
* **تعدادِ نمایش**: `displayCount=3` سه‌تا، `0` برابرِ ظرفیت.
* **بنرها**: جایگاهِ نافروخته نامرئی؛ بنرِ راست فقط خودش را نشان می‌دهد و چپ/پایین را فعال نمی‌کند؛ فهرستِ فروشگاه‌ها از بنرِ کنارش اثر نمی‌گیرد.
* **رندرِ صفحه**: هر سه سکشن در HTML حاضرند و هیچ سبکِ سرریزِ افقی‌ای نیست. جداگانه هم راستی‌آزمایی شد که قابِ بنرِ پایین فقط در صفحه‌ی اصلی رندر می‌شود (`/clubs` و `/shop` ندارندش).

## Review Fixes (فاز ۵)

بازبینیِ خصمانه‌ی چندعاملی اجرا شد (۲۰ یافته)؛ موارد واقعی رفع و بازتست شدند:

* **جعلِ تفکیکِ نوعِ میز** — تعدادِ کل را واقعی کرده بودم ولی کارت هنوز خودش آن را ۵۰/۳۰/۲۰ بینِ اسنوکر/پاکت/هی‌بال تقسیم می‌کرد. حالا تفکیک هم از ستون‌های واقعی می‌آید (میزِ VIP به نوعِ خودش اضافه می‌شود؛ ایرهاکی عمداً بیرون است چون میزِ بیلیارد نیست).
* **جعلِ امتیاز در کارتِ فروشنده** — همان باگی که برای باشگاه رفع شده بود، برای فروشگاه‌ها مانده بود (امتیازِ ثابتِ ۵ با ۰ نظر). حالا پنهان می‌شود.
* **خالی‌شدنِ بی‌صدای بنرِ فروخته‌شده** — اگر ادمین اشتباهاً جایگاهِ بنری را روی «رایگان» می‌گذاشت، کمپینِ پولی بدونِ هیچ هشداری از صفحه حذف می‌شد. حالا جایگاهِ بنری در حالتِ رایگان همچنان کمپین‌هایش را سرو می‌کند.
* **داده‌ی نمونه در حالتِ دستی/پولی** — جایگاهی که ادمین رویش «دستی» گذاشته ولی هنوز کمپینی نساخته، به آرایه‌ی نمونه برمی‌گشت؛ یعنی ادمین جایگاه را خالی می‌دید ولی کاربر فهرستی پر. حالا سکشن حذف می‌شود.
* **ظرفیتِ صفر** — متضادِ هشدارِ خودِ پنل، داده‌ی نمونه نشان می‌داد. حالا واقعاً هیچ.
* **تخفیفِ ۱۰۰٪** — تقسیم بر صفر و قیمتِ خط‌خورده‌ی `Infinity` (در هر دو مسیرِ رایگان و کمپینی).
* موارد کوچک‌تر: فاصله‌ی بنرِ پایین از اسلایدرِ تیره و فوتر، پنهان‌شدنِ تیترِ «فروشندگان تجهیزات» وقتی فقط بنرِ کناری زنده است، لاگ‌شدنِ خطای محتوای رایگان (تا خرابیِ کوئری شبیهِ «محتوا نداریم» دیده نشود)، و توضیحِ جایگاهِ بنرِ پایین که هنوز «در همه‌ی صفحات» می‌گفت.

تست‌ها بعد از این اصلاحات دوباره اجرا شدند: **۳۷/۳۷ پاس** (سه تستِ تازه برای قاعده‌ی «داده‌ی نمونه فقط در حالتِ رایگان»).

## Known Issues (فاز ۵)

* **داده‌ی واقعیِ کم**: امروز ۸ محصول و ۸ باشگاه در دیتابیس است و جدولِ فروشگاه‌ها **خالی**. پس سکشنِ فروشگاه‌ها همچنان به آرایه‌ی نمونه‌ی قبلی برمی‌گردد تا اولین فروشگاهِ واقعی تأیید شود. محصولات و باشگاه‌ها از همین حالا واقعی‌اند.
* **SSR**: محتوای جایگاه‌ها سمتِ کلاینت گرفته می‌شود، پس HTMLِ اولیه هنوز آرایه‌ی نمونه را دارد و بعد از هیدریشن با داده‌ی واقعی عوض می‌شود. برای کاربر تقریباً نامحسوس است ولی برای موتورهای جست‌وجو یعنی محتوای واقعی دیده نمی‌شود — انتقالِ این سه سکشن به رندرِ سروری پیشنهادِ فاز ۶ است.
* کشِ کلاینت ۹۰ ثانیه است؛ تغییرِ ادمین تا یک‌ونیم دقیقه بعد در نشستِ باز دیده می‌شود (رفرش فوری اعمالش می‌کند).

## Next Phase

PHASE 6 — (منتظر دستور شما)

---

# PHASE 4 — ADMIN ADVERTISING MANAGEMENT & PRICING (آرشیو)

COMPLETED — ۱۴۰۵/۰۵/۰۶ (2026-07-28) · کامیت‌های `33b4b32`، `3c9f741`، `48274ba`

---

## Admin Features (فاز ۴)

پنلِ `/admin/advertising` به یک مرکزِ کاملِ مدیریتِ تبلیغات تبدیل شد:

* **کنترل‌های مستقلِ هر جایگاه در یک ردیف**: فعال/غیرفعال · حالت (رایگان/دستی/پولی) · ظرفیت · **تعدادِ نمایش** · **حالتِ چرخش** · **اولویت** · قیمتِ پایه · مدت. تغییر هر کدام روی بقیه اثر ندارد (تست شد).
* **همچنان بدونِ هیچ Master Toggle**: هیچ دکمه‌ای برای «روشن‌کردنِ همه‌ی تبلیغات» وجود ندارد؛ فعال‌کردنِ یک جایگاه هیچ جایگاهِ دیگری را فعال نمی‌کند و کلیدِ قدیمیِ `ad_slots_enabled` همچنان بی‌اثر است.
* **مدیریتِ کمپین‌ها** (از فاز ۲): ساخت بنری/موجودیتی، تغییر بینِ ۸ وضعیت، حذف.
* **درخواست‌های تبلیغ**: فهرست و بستن.
* **پنلِ آمار** در بالای صفحه (بخشِ Analytics).

## Pricing Features (فاز ۴)

* **CRUD کاملِ پلن**: ساخت، ویرایشِ قیمت/مدت/تعدادِ آگهی/**مقدارِ اعتبار**، **جابه‌جایی بینِ جایگاه‌ها**، غیرفعال‌سازی، و **حذف**. حذف هوشمند است: پلنی که سفارشِ ثبت‌شده دارد (FK از `campaign_orders`/`ad_credits`) هرگز حذف نمی‌شود و فقط غیرفعال می‌گردد — تاریخچه‌ی مالی سالم می‌ماند.
* **هیچ قیمتی در کد نیست**؛ همه از جدولِ `ad_pricing_plans` خوانده می‌شود و از پنل قابلِ تغییر است.
* پلن‌ها در پنل بر اساسِ جایگاه گروه‌بندی می‌شوند تا پله‌های مدتِ هر جایگاه کنار هم دیده شوند.
* **۱۴ پلنِ پیش‌فرض** (مایگریشن ۰۱۸) دقیقاً طبقِ تعرفه‌ی مصوب:

| جایگاه | پله‌ها (تومان) |
|---|---|
| محصولات ویژه‌ی بازار | ۷ روزه ۳۹۹٬۰۰۰ · ۳۰ روزه ۹۹۹٬۰۰۰ · پرمیوم ۳۰ روزه ۱٬۹۹۰٬۰۰۰ |
| باشگاه‌های پیشنهادی | ۳۰ روزه ۹۹۹٬۰۰۰ |
| فروشگاه‌های تجهیزات | ۳۰ روزه ۹۹۹٬۰۰۰ |
| بنر کناری راست | ۷ روزه ۷۹۹٬۰۰۰ · ۳۰ روزه ۲٬۴۹۰٬۰۰۰ · ۳ ماهه ۵٬۹۹۰٬۰۰۰ |
| بنر کناری چپ | همان قیمت‌های راست |
| بنر پایین صفحه | ۷ روزه ۱٬۴۹۰٬۰۰۰ · ۳۰ روزه ۴٬۹۹۰٬۰۰۰ · ۳ ماهه ۱۲٬۹۹۰٬۰۰۰ |

## Placement Controls (فاز ۴)

| کنترل | معنا |
|---|---|
| Active / Inactive | نمایشِ جایگاه؛ کاملاً مستقل |
| Free / Manual / Paid | حالتِ تأمینِ محتوا؛ فقط `paid` قابلِ خرید است |
| Capacity | چند کمپین می‌تواند در این جایگاه رزرو شود (سقفِ سخت) |
| **Display Count** | چند کمپین هم‌زمان دیده شود (۰ = به‌اندازه‌ی ظرفیت) |
| **Rotation Mode** | عادلانه / وزنی / تصادفی / ثابت |
| **Priority** | اولویتِ جایگاه؛ بزرگ‌تر زودتر پردازش/فهرست می‌شود |

**گیتِ Paid**: کاتالوگِ خرید (`/api/ads/placements?catalog=1`) فقط جایگاه‌های `paid` را با پله‌های قیمتشان برمی‌گرداند. جایگاهِ رایگان/دستی اصلاً گزینه‌ی خرید ندارد، و پولی‌کردنِ یکی هیچ جایگاهِ دیگری را قابلِ خرید نمی‌کند (تست شد). اگر هیچ جایگاهی پولی نباشد، صفحه‌ی درخواستِ تبلیغ به‌جای کشوی خالی، توضیحِ روشن نشان می‌دهد.

## Rotation (فاز ۴)

چرخش **سمتِ سرور** تصمیم‌گیری می‌شود و کلاینت فقط در همان ترتیب می‌چرخد:

* **fair** — کم‌تحویل‌ترین اول، بر مبنای **نرخِ تحویل** (`serves ÷ ساعت‌های عمرِ کمپین ÷ وزن`) به‌علاوه‌ی **نویزِ تصادفیِ کنترل‌شده** که دامنه‌اش با پراکندگیِ همان کلیدها مقیاس می‌گیرد. نتیجه: **جایگاهِ ثابتِ اول یا دوم وجود ندارد**. پیش‌فرضِ کاروسل‌های بنری.
* **weighted** — قرعه‌کشیِ بدونِ جایگذاری به‌نسبتِ وزن.
* **random** — بُرزدنِ کامل.
* **fixed** — ترتیبِ ثابتِ `sort_order`. **پیش‌فرضِ سکشن‌های موجودیتی** (محصولات/باشگاه‌ها/فروشگاه‌ها) چون آن‌ها چیدمانِ انتخابیِ ادمین‌اند نه کاروسلِ فروخته‌شده، و نباید در هر بارگذاری بُر بخورند.

سه نکته‌ی مهمِ پیاده‌سازی (هر سه از بازبینیِ خصمانه بیرون آمدند):

۱. **مبنای عدالت `serves` است، نه `impressions`** — ستونِ تازه‌ای که فقط خودِ سرور هنگامِ تحویلِ واقعی زیادش می‌کند (`bh_bump_serves`، مایگریشن ۰۱۹). `impressions` را بیکنِ **بدونِ احرازِ هویت** می‌نویسد، پس اگر مبنای چرخش بود، هر کسی می‌توانست با چند درخواستِ ساده شمارشِ یک کمپینِ پولی را باد کند و آن را تا پایانِ دوره‌اش ته صف بفرستد. `impressions`/`clicks` فقط برای گزارشِ آماری ماندند.

۲. **نرخ، نه شمارشِ خام** — اگر خام مقایسه می‌شد، کمپینی که یک ماه می‌چرخید با پیوستنِ هر کمپینِ تازه تا مدت‌ها صفر تحویل می‌گرفت؛ یعنی تبلیغ‌کننده‌ای که پول داده از چرخه بیرون می‌افتاد. نرخ با گذشتِ زمان خودش تصحیح می‌شود و هیچ‌کس برای همیشه گرسنه نمی‌ماند.

۳. **اول جمع، بعد چرخش، آخر بریدن** — پیش‌تر بریدنِ فهرست هنگامِ جمع‌آوری انجام می‌شد، پس همیشه همان چند کمپینِ اولِ `sort_order` برنده بودند و چرخش بی‌اثر می‌شد.

کشِ کلاینت هم عمرِ ۹۰ ثانیه‌ای گرفت: کشِ همیشگی یعنی کاربری که ساعت‌ها در SPA می‌ماند تا آخر همان یک ترتیب را می‌بیند و عدالتِ چرخش برایش اتفاق نمی‌افتد.

## Analytics (فاز ۴)

پنلِ آمار در بالای صفحه‌ی تبلیغات: **کمپین‌های فعال / در انتظار / منقضی** (به‌علاوه‌ی زمان‌بندی‌شده، پیش‌نویس، رد/لغو)، **نمایش**، **کلیک**، **CTR**، و **درآمدِ کل** از سفارش‌های پرداخت‌شده — به‌همراه جدولِ تفکیکیِ هر جایگاه (فعال/نمایش/کلیک/CTR). همه‌ی اعداد فارسی‌اند. تا وقتی خریدِ آنلاینِ جایگاه (فاز ۵) فعال نشده، درآمد طبیعتاً صفر است و پنل همین را توضیح می‌دهد.

## Database Changes (فاز ۴)

* **۰۱۸** — `placements`: `display_count` (پیش‌فرض ۰)، `rotation_mode` (CHECK چهار مقدار)، `priority` (پیش‌فرض ۰). `ad_pricing_plans`: `credit_amount` + ایندکسِ `(placement_key, sort_order) WHERE is_active`. ۱۴ پلنِ پیش‌فرضِ جایگاه‌محور + پرکردنِ اعتبارِ پلن‌های عمومیِ فاز ۲. بخشِ داده با مارکرِ `mig_018_data_done` یک‌بار اجرا می‌شود تا ویرایش‌های ادمین بازنویسی نشوند (ایدمپوتنسی تست شد: اجرای دوباره، ۱۹ پلن را ۱۹ نگه داشت).
* **۰۱۹** — `campaigns.serves` (شمارنده‌ی تحویلِ سرورمحور) + تابعِ `bh_bump_serves(uuid[])` با `REVOKE ... FROM PUBLIC, anon, authenticated`؛ و تنظیمِ پیش‌فرضِ سه جایگاهِ موجودیتی روی `fixed` (مارکرِ `mig_019_data_done`).

## Tests (فاز ۴)

**۴۰/۴۰ پاس** (علیه بیلدِ production محلی + دیتابیسِ زنده، با بازگرداندنِ کاملِ وضعیت و صفر باقی‌مانده): گاردِ ادمین (۴۰۱/۴۰۳)، ذخیره‌ی سه کنترلِ تازه، ردِ حالتِ چرخشِ نامعتبر، **استقلالِ کنترل‌ها**، اثرِ اولویت بر ترتیب، **نبودِ Master Toggle**، صحتِ **هر ۱۴ قیمتِ پیش‌فرض**، CRUD کاملِ پلن شاملِ اعتبار و جابه‌جاییِ جایگاه، **حذفِ واقعیِ پلنِ بی‌سفارش** و **غیرفعال‌سازیِ پلنِ دارای سفارش**، گیتِ Paid در **هر دو مسیرِ جدید و قدیمی** و در **ثبتِ درخواستِ تبلیغ**، **چرخشِ عادلانه** (هر ۴ کمپین در ۴۰ درخواست دیده شدند، هیچ‌کدام کمتر از ۳ بار)، عقب‌افتادنِ کمپینِ پرتحویل، **مسدودبودنِ حمله‌ی بیکن** (باد کردنِ ۹۰۰٬۰۰۰ نمایشِ کلاینتی نتوانست کمپین را حذف کند)، شمارشِ تحویلِ سرورمحور، پایداریِ حالتِ ثابت، `display_count` و بازگشتش به ظرفیت، و همه‌ی محاسباتِ آمار.

به‌علاوه **۵ تستِ مرزی**: عددِ منفی (اولویتِ ‎−۳ منفی می‌ماند، شمارشِ منفی صفر می‌شود)، عددِ غول‌آسا (کلمپ به سقف به‌جای خطای ۵۰۰)، ارقامِ فارسی، آرایه در `rotationMode` (۴۰۰ به‌جای ۵۰۰)، و آبجکت در فیلدِ عددی.

## Review Fixes (فاز ۴)

بازبینیِ خصمانه‌ی چندعاملی اجرا شد؛ یافته‌های تأییدشده همه رفع و بازتست شدند:

* **بحرانی — هدایتِ تحویل با بیکنِ بدونِ احرازِ هویت**: چرخشِ عادلانه به شمارنده‌ای تکیه داشت که هر کسی می‌توانست بنویسدش ⇒ سرکوبِ دائمیِ یک کمپینِ پولی. رفع با شمارنده‌ی `serves` سرورمحور (مایگریشن ۰۱۹).
* **مهم — گرسنگیِ کمپینِ قدیمی**: مقایسه‌ی خامِ شمارش، کمپینِ پرسابقه را با پیوستنِ هر کمپینِ تازه از چرخه بیرون می‌انداخت. رفع با معیارِ نرخ.
* **مهم — دورزدنِ گیتِ Paid از مسیرِ قدیمی** `/api/ads/slots?catalog=1` و از **ثبتِ درخواستِ تبلیغ**. هر دو حالا سرورساید گیت دارند.
* **مهم — کم‌گزارشیِ آمار**: KPIها روی برشِ ۵۰۰ ردیفیِ پنل حساب می‌شدند. حالا `adStats` خودش صفحه‌به‌صفحه تا تهِ جدول می‌خواند.
* **مهم — بُرخوردنِ چیدمانِ ادمین در سکشن‌های موجودیتی**: آن‌ها بیکن نمی‌فرستند، پس `fair` عملاً تصادفیِ محض می‌شد. پیش‌فرضشان `fixed` شد.
* **جزئی** — fail-open بودنِ گاردِ حذفِ پلن (حالا fail-closed)، پیش‌فرضِ دست‌نیافتنیِ `num()`، کشِ همیشگیِ کلاینت (TTL ۹۰ ثانیه)، و نبودِ هشدارِ «تعدادِ نمایش بیش از ظرفیت» در پنل.

## Known Issues (فاز ۴)

* خریدِ آنلاینِ جایگاه هنوز فعال نیست؛ `/advertise` فرمِ درخواست است و تعرفه‌ها را نشان می‌دهد. اتصالِ پرداخت + مصرفِ `credit_amount` در فاز ۵.
* `campaign_orders`/`ad_credits` همچنان فقط اسکیما هستند (به‌جز رکوردهای تستی که پاک شدند)، پس «درآمد» تا فاز ۵ صفر می‌ماند.
* شمارنده‌ی `impressions`/`clicks` هنوز از بیکنِ بدونِ auth می‌آید و برای **صورتحساب** قابلِ اتکا نیست (سخت‌گیری در فاز ۶). ولی از فاز ۴ به بعد **تحویل** را هدایت نمی‌کند، پس دیگر سطحِ حمله نیست.
* `priority` فقط ترتیبِ پردازش/فهرست را عوض می‌کند؛ جایگاه‌ها با هم رقابتِ نمایش ندارند چون هر کدام ناحیه‌ی جدا از صفحه‌اند.
* `serves` هر بار که سرور جایگاهِ `fair` را تحویل می‌دهد یک واحد بالا می‌رود (یک UPDATE در هر درخواستِ صفحه). اگر ترافیک خیلی زیاد شد، این را می‌شود دسته‌ای یا نمونه‌گیری‌شده کرد — فعلاً لازم نیست.

## Next Phase

PHASE 5 — (منتظر دستور شما)

---

# PHASE 3 — IDENTITY-BASED FREE AD QUOTA (آرشیو)

COMPLETED — ۱۴۰۵/۰۵/۰۶ (2026-07-28) · کامیت `1c24a55`

---

## Decision D3 — PHASE 3 Security Finding — Closed

تصمیمِ ثبت‌شده‌ی مالک — ۱۴۰۵/۰۵/۰۶ (2026-07-28). این یادداشت بر هر برداشتِ دیگری از بخشِ «Security (فاز ۳)» در همین سند مقدم است.

**وضعیت: Closed.**

* حفره‌ی امنیتی — نبودِ RLS روی `users`/`clubs`/`tables` و در نتیجه امکانِ خواندن/نوشتنِ آن‌ها با کلیدِ عمومیِ Supabase — شناسایی و در **مایگریشن ۰۱۷** اصلاح شد.
* **زمینه‌ی واقعیِ پروژه:** در تمامِ مدتی که این حفره وجود داشت، پروژه در محیطِ **خصوصی/Preview** روی Vercel توسعه و تست می‌شد و **فقط توسطِ مالکِ پروژه** استفاده شده است؛ به‌صورتِ عمومی منتشر نشده و نشانیِ آن در اختیارِ شخصِ دیگری نبوده است.
* بنابراین **هیچ Incident Response یا اقدامِ اضطراری برای کاربران لازم نیست** و هیچ‌کدام از موارد زیر انجام نشده و نباید انجام شود: بازنشانیِ اجباریِ رمز، باطل‌کردنِ نشست‌ها، خروجِ اجباریِ کاربران، چرخاندنِ Secret/Credential، حذف یا تغییرِ داده، و ساختِ مایگریشنِ تازه برای این موضوع.
* بررسیِ گسترده‌ی Incident یا جست‌وجوی سوءاستفاده‌ی واقعی نیز در این مرحله لازم تشخیص داده نشد.
* **اصلاحاتِ مایگریشن ۰۱۷ الزامِ قطعیِ Production هستند** و باید دست‌نخورده بمانند.

### الزاماتِ قطعیِ Production (معیارِ پذیرشِ همه‌ی فازهای بعدی)

۱. RLS روی جداولِ حساس همیشه فعال بماند.
۲. کلاینت هیچ دسترسیِ مستقیمی به داده‌های حساس نداشته باشد.
۳. کد ملی فقط از مسیرِ امنِ سرورساید مدیریت شود.
۴. نقش و تأیید (Verification) فقط از بک‌اند و با Enforcement سمتِ سرور کنترل شود.
۵. کاربر نتواند خودش را Admin یا Verified کند.
۶. هیچ منطقِ امنیتیِ مهمی صرفاً به کلاینت یا localStorage اعتماد نکند.
۷. همه‌ی عملیاتِ حساس سرورساید و با Authorizationِ درست کنترل شوند.
۸. کلیدِ عمومیِ Supabase هرگز نقشِ مجوزِ دسترسی به داده‌های حساس را نداشته باشد.

### اقدامِ باقی‌مانده

پیش از عمومی‌شدنِ نهاییِ سایت، یک **Security Audit نهایی در PHASE 7** انجام شود.

---

## Identity Architecture (فاز ۳)

```
persons  (شخصِ حقیقی — واحدِ سهمیه)
  id uuid PK
  national_id_hash text UNIQUE   ← HMAC-SHA256 کد ملیِ نرمال‌شده
  is_test boolean                ← پرچمِ حساب‌های تستیِ مالک (بی‌اثر در منطق)
     ▲
     │ users.person_id  (FK، ON DELETE SET NULL — «لینک» است نه Merge)
     │
  چند حساب ← یک شخص ← یک سهمیه‌ی مشترک
```

* **کلیدِ هش عمداً از env نمی‌آید**، از `app_settings.person_hash_salt` (جدولی با RLS و بدون policy ⇒ فقط service-role) خوانده می‌شود. اگر کلید از `JWT_SECRET` می‌آمد، هر محیطی با secret متفاوت برای یک کد ملی هشِ متفاوت می‌ساخت و همان شخص دو ردیف persons و دو برابر سهمیه می‌گرفت. `NID_HMAC_SECRET` در صورت تنظیم مقدم است (برای چرخشِ دستی).
* خودِ کد ملی هرگز در `persons` ذخیره نمی‌شود؛ فقط هش.
* اتصال در سه نقطه: ثبت‌نام (پس از تأییدِ شاهکار)، مسیرِ احرازِ کد ملیِ حساب‌های قدیمی، و بک‌فیلِ تنبل در کرانِ روزانه. بک‌فیل **عمداً روی سرورِ پروداکشن** اجرا می‌شود تا هش با نمکِ همان دیتابیس ساخته شود.
* **تصمیم D2 رعایت شد**: هر دو حسابِ تستیِ مالک دست‌نخورده باقی‌اند؛ هیچ Merge/حذفی در مایگریشن نیست و اتصال با NULL کردنِ `person_id` برگشت‌پذیر است.
  * وضعِ فعلی روی دیتابیس (پس از بک‌فیلِ پروداکشن): `09001327283` کد ملیِ **تأییدشده** دارد ⇒ به شخص وصل شد. `09121327283` کد ملی دارد ولی `national_id_verified=false` ⇒ هنوز شخص ندارد و سهمیه‌ی رایگان نمی‌گیرد (طبقِ قانونِ «هویتِ تأییدنشده سهمیه نمی‌سازد»). اگر بخواهید هر دو یک سهمیه‌ی مشترک داشته باشند، کافی است هویتِ حسابِ دوم از مسیرِ عادیِ احراز تأیید شود؛ خودکار به همان شخص وصل می‌شود.

## Role Logic (فاز ۳)

نقشِ «تأییدشده» فقط از منابعِ سرورساید خوانده می‌شود — `primaryRole`/`secondaryRoles` خوداظهاری‌اند و **هیچ اثری بر سهمیه ندارند**:

| نقش | منبعِ تأیید |
|---|---|
| user | همه دارند (پایه) |
| player · coach · referee · technician · seller · manufacturer | ردیفِ `profiles` با `status='approved'` **و** `verified=true` |
| club_owner | مالکیتِ باشگاهی با `verificationStatus='verified'` (تأییدِ جواز با کد ملیِ مالک) |

**چرا شرطِ `verified` هم لازم بود:** DDLِ جدولِ profiles برای `status` پیش‌فرضِ `'approved'` دارد و کاربر با ساختنِ پروفایل، خودش approved می‌شد؛ بدونِ شرطِ تیکِ ادمین (`verified`)، هر کاربری با ساختنِ پروفایلِ فروشنده به سهمیه‌ی ۴تایی می‌رسید.

نقش‌های یک شخص از **همه‌ی حساب‌هایش** جمع‌آوری می‌شود (نه فقط حسابِ جاری).

## Quota Logic (فاز ۳)

* **Free Quota = بیشینه‌ی سهمیه بینِ نقش‌های تأییدشده** (جمع نمی‌شوند): user/player/referee=۱ · technician/coach=۲ · club_owner/manufacturer/seller=۴ — دوره‌ی **۳۰ روزه**. اعداد در `app_settings.ads_free_quota` و از پنل ادمین قابل ویرایش‌اند (هاردکد نیستند).
* مصرف در دفترِ `quota_consumptions` ثبت می‌شود، نه با شمارشِ آگهی‌های زنده ⇒ **حذف یا انقضای آگهی سهمیه را برنمی‌گرداند** (چرخه‌ی ثبت→حذف→ثبت بسته شد).
* مصرف **اتمیک** است: `bh_consume_quota` با قفلِ ردیفِ شخص (FOR UPDATE) بررسی و ثبت را در یک تراکنش انجام می‌دهد؛ دو درخواستِ هم‌زمان حتی از دو حسابِ یک شخص نمی‌توانند از سقف رد شوند.
* **ارتقای نقش** سقف را بالا می‌برد ولی مصرفِ قبلی ریست نمی‌شود؛ **تنزلِ نقش** هم مصرف را پاک نمی‌کند (شخصِ مازادمصرف تا خروجِ مصرف‌ها از پنجره مسدود می‌ماند) ⇒ چرخاندنِ مکررِ نقش سودی ندارد.
* حسابِ بدونِ کد ملیِ تأییدشده شخص ندارد ⇒ سهمیه‌ی رایگان ندارد (`identityRequired` با راهنمای تأییدِ هویت در UI).
* دو مسیرِ ثبتِ آگهی (`/api/market/ads` و `/api/products`) هر دو از همین دروازه رد می‌شوند.
* `resetAt` زمانِ بازشدنِ **اولین جای خالی** است (با در نظر گرفتنِ مصرفِ مازاد)، نه صرفاً قدیمی‌ترین مصرف.
* مسیرِ پلنِ خریداری‌شده عمداً دست‌نخورده ماند (per-account، شمارشِ products) — انتقالش به اعتبارهای شخص‌محور در فاز ۵.

## Database Changes (فاز ۳)

* **۰۱۶**: جدولِ `persons`، ستون + FK + ایندکسِ `users.person_id`، جدولِ `quota_consumptions`، تابعِ `bh_consume_quota` (با `REVOKE ... FROM PUBLIC, anon, authenticated`)، و مقداردهیِ یک‌باره‌ی `ads_free_quota` با اعدادِ فاز ۳ (گیت‌شده با مارکرِ `mig_016_data_done` تا ویرایشِ بعدیِ ادمین بازنویسی نشود). RLS روی هر دو جدولِ جدید روشن و بدون policy.
* **۰۱۷ (امنیتی)**: `users`/`clubs`/`tables` هرگز RLS نداشتند و گرنتِ anon داشتند.
* `app_settings.person_hash_salt` — نمکِ هشِ اشخاص (خودکار در اولین اجرا ساخته می‌شود).

## Security (فاز ۳)

* **حفره‌ی بحرانیِ کشف‌شده و بسته‌شده** (وضعیتِ نهایی و تعیین‌تکلیف در **Decision D3** بالا — Closed، بدونِ نیاز به Incident Response): کلیدِ `anon` داخلِ باندلِ مرورگر منتشر می‌شود و سه جدولِ پایه RLS نداشتند. اندازه‌گیریِ زنده پیش از اصلاح: `GET /rest/v1/users` → ۲۰۰ با ردیف‌های واقعی (کد ملی، هشِ رمز، کدِ OTP) و `PATCH /rest/v1/users` → ۲۰۴. یعنی در آن وضعیت، دارنده‌ی کلید می‌توانست کد ملیِ خود را عوض کند (سهمیه‌ی تازه)، باشگاهی را verified کند (سهمیه‌ی ۴تایی) یا خودش را admin کند. پس از مایگریشن ۰۱۷: هر چهار مسیر ۴۰۱ می‌دهند و `products` فقط خواندنی مانده. Storage و Realtime دست‌نخورده کار می‌کنند. طبقِ D3، پروژه در این بازه خصوصی/Preview و فقط در دستِ مالک بوده است.
* خودتأییدیِ باشگاه بسته شد: `PUT /api/clubs/[id]` دیگر بدنه‌ی خام را UPDATE نمی‌کند؛ فیلدهای اعتماد (`verificationStatus`، `licenseVerified`، `licenseCheckedAt`، `licenseNumber`، `ibanVerified`، `ibanOwnerName`، `ownerId`، `id`، `createdAt`) برای غیرادمین حذف می‌شوند.
* دروازه‌ی سهمیه هنگام خطای دیتابیس **fail-closed** است (۵۰۳)، نه fail-open؛ ولی «هویت تأیید نشده» از «خطای زیرساخت» تفکیک شده تا کاربرِ تأییدشده پیامِ اشتباه نبیند.
* کد ملی همچنان فقط به مالکِ حساب نمایش داده می‌شود و از هیچ مسیری با ورودیِ کلاینت نوشته نمی‌شود (تست شد).
* تابعِ مصرف از دسترسِ anon خارج است (تست: ۴۰۱).

## Tests (فاز ۳)

**۳۷/۳۷ سناریوی موظف** (پوششِ هر ۱۷ موردِ خواسته‌شده) + **۱۴ تستِ تکمیلی** برای اصلاحاتِ بازبینی — همه علیه بیلدِ production و دیتابیسِ زنده، با پاک‌سازیِ کاملِ فیکسچرها:

۱ فقط user=۱ · ۲ player=۱ · ۳ technician=۲ · ۴ coach=۲ · ۵ باشگاه‌دارِ verified=۴ · ۶ همه‌ی ۸ نقش ⇒ ۴ (نه ۲۱) · ۷ نقشِ تأییدنشده بی‌اثر · ۸ تیکِ ادمین ⇒ ۴ · ۹ و ۱۰ دو حساب با یک کد ملی ⇒ یک شخص و سهمیه‌ی مشترک (هر دو حساب سالم) · ۱۱ مصرفِ واقعی از API · ۱۲ حذفِ آگهی بدونِ برگشتِ سهمیه · ۱۳ انقضا بدونِ برگشت · ۱۴ دوره‌ی ۳۰ روزه‌ی تازه · ۱۵ ارتقای نقش بدونِ ریستِ مصرف · ۱۶ تنزلِ نقشِ امن · ۱۷ تلاش‌های دور زدن (نقشِ خوداظهاری، بدونِ هویت، دستکاریِ کد ملی/person_id، صدا زدنِ تابع با کلیدِ anon، و رقابتِ هم‌زمان از دو حسابِ یک شخص که فقط یکی برنده می‌شود).

تکمیلی: دروازه‌ی `/api/products`، ممنوعیتِ خودتأییدیِ باشگاه (و مجازبودنِ ادمین)، پایداریِ هشِ نمکِ دیتابیس، و صحتِ `resetAt` هنگام مصرفِ مازاد.

## Known Issues (فاز ۳)

* ~~نیاز به تصمیمِ مالک درباره‌ی چرخاندنِ رمزها/نشست‌ها~~ — **تعیین‌تکلیف شد**: طبقِ **Decision D3**، پروژه در آن بازه خصوصی/Preview و فقط در دستِ مالک بوده؛ هیچ اقدامِ Incident Response لازم نیست و هیچ‌کدام انجام نشده است. این مورد بسته است.
* `ads_quota_enabled` هنوز **خاموش** است (رفتار فعلی حفظ شد). تا روشن‌شدنش هیچ کاربری مسدود نمی‌شود، ولی مصرف‌ها از همین حالا در دفتر ثبت می‌شوند تا تاریخچه واقعی باشد.
* سهمیه‌ی استوری هنوز per-account و بر پایه‌ی نقشِ خوداظهاری است (فقط باگِ «صفر یعنی نامحدود» در انتخابِ نقش اصلاح شد)؛ شخص‌محورکردنش در فازِ بعدی.
* پلنِ خریداری‌شده هنوز per-account است و حذفِ آگهی در پنجره‌اش سهمیه را برمی‌گرداند — تا فاز ۵ پذیرفته شده.
* اگر پردازش بینِ «مصرفِ سهمیه» و «درجِ آگهی» قطع شود (کرشِ سرور)، یک سهمیه می‌سوزد؛ مسیرِ جبران فقط خطای دیتابیس را پوشش می‌دهد.

## Next Phase

PHASE 4 — (منتظر دستور شما) · پیشنهاد: انتقالِ بسته‌های پولی به اعتبارهای شخص‌محور + ledger، سپس پرداختِ جایگاه‌های تبلیغاتی.

**PHASE 7 — Security Audit نهایی (الزامی پیش از عمومی‌شدنِ سایت):** طبقِ Decision D3، پیش از انتشارِ عمومی باید یک ممیزیِ امنیتیِ نهایی انجام شود که دستِ‌کم هر هشت الزامِ قطعیِ Production (بخشِ D3) را روی محیطِ واقعی راستی‌آزمایی کند.

---

# PHASE 2 — CORE ADVERTISING SYSTEM (آرشیو)

COMPLETED — ۱۴۰۵/۰۵/۰۶ (2026-07-28)

هستهٔ Placement/Campaign/Pricing/Expiration ساخته، تست، بازبینی و روی دیتابیس زنده اعمال شد. مطابق تصمیمات D1/D2 (بخش Decisions فاز ۱ در ادامهٔ همین سند).

## Completed (فاز ۲)

* **مایگریشن 015** (`supabase/migrations/015_advertising_core.sql`) — روی دیتابیس زنده اعمال شده: جدول‌های `placements` (۶ جایگاه مستقل)، `campaigns` (۸ وضعیت)، `ad_pricing_plans` (پلن‌های DB-driven)، `ad_credits` (اسکیمای اعتبار آگهی)، `campaign_orders` (اسکیمای سفارش، ایندکس یکتای `provider_authority`).
* **توابع اتمیک**: `bh_expire_campaigns()` (SCHEDULED→ACTIVE و ACTIVE/SCHEDULED/PENDING_*→EXPIRED؛ DRAFT عمداً مستثنا) و `bh_track_campaign()` (شمارش اتمیک فقط برای کمپین ACTIVE داخل پنجرهٔ زمانی) — هر دو با `REVOKE ... FROM PUBLIC, anon, authenticated` (تأییدشده در ACL زنده).
* **بدون Master Toggle**: هر جایگاه `is_active` و `mode` (free/manual/paid) مستقل دارد؛ `ad_slots_enabled` از whitelist ادمین حذف شد و در تست ۴ ثابت شد بی‌اثر است.
* **API عمومی** `/api/ads/placements` (+`?catalog=1`) با resolve سروری موجودیت‌ها (محصول/باشگاه/فروشنده → snapshot کارت)؛ **آداپتور سازگاری** `/api/ads/slots` (DEPRECATED، کلیدهای legacy برای باندل‌های کش‌شده)؛ **API ادمین** `/api/admin/advertising`؛ **کران** `/api/cron/expire-campaigns` (روزانه، vercel.json).
* **پنل ادمین جدید** `/admin/advertising`: کنترل مستقل هر جایگاه (فعال/غیرفعال + حالت + ظرفیت/قیمت/مدت)، مدیریت کمپین‌ها (بنری/موجودیتی، تغییر وضعیت بین ۸ حالت، حذف)، ویرایش پلن‌های قیمت‌گذاری، فهرست درخواست‌های تبلیغات. `/admin/ad-slots` → redirect.
* **AdSlot v2**: چرخش اسلایدری وزن‌دار برای جایگاه بنری، برچسب شفافیت «تبلیغ»، شمارش impression/click با sendBeacon، هوک `usePlacement` برای سکشن‌های ویژه.
* **چیدمان D1** در صفحهٔ اصلی: بازار = ۱۴ محصول بدون بنر کناری؛ فروشگاه‌های تجهیزات = راست/اصلی/چپ در Desktop و ستونی بدون Overflow در Mobile؛ بنر پایینی در layout. سه سکشن ویژه placement-driven شدند با fallback به آرایه‌های فعلی وقتی جایگاه خالی/غیرفعال است.
* **بازبینی خصمانه** (۳ بازبین + راستی‌آزماها) و رفع همهٔ یافته‌های تأییدشده: CSRF بیکن (CSRF_EXEMPT در proxy)، XSS از `javascript:` در link_url (اعتبارسنجی + safeHref)، extendDays (تمدید از انتهای فعلی نه جایگزینی)، await روی tracking/expiry در سرورلس، فیلتر UUID در resolve (یک ref خراب کل کوئری را نمی‌کشد)، گیت idempotency مهاجرت داده با مارکر، بازنویسی CSS گرید (بدون `:has`، بدون gap باقی‌مانده)، dedupe کمپین‌های هم‌مرجع.

## Files Changed (فاز ۲)

* **جدید:** `supabase/migrations/015_advertising_core.sql`، `apps/web/lib/ads/core.ts`، `apps/web/lib/ads/resolve.ts`، `apps/web/app/api/ads/placements/route.ts`، `apps/web/app/api/admin/advertising/route.ts`، `apps/web/app/admin/advertising/page.tsx`، `apps/web/app/api/cron/expire-campaigns/route.ts`
* **تغییر:** `components/ads/AdSlot.tsx` (بازنویسی v2)، `app/page.tsx` (چیدمان D1 + سکشن‌های placement-driven)، `app/layout.tsx`، `app/advertise/page.tsx` (کاتالوگ از placements)، `app/api/ads/slots/route.ts` (آداپتور legacy)، `app/api/ads/requests/route.ts`، `app/admin/ad-slots/page.tsx` (redirect)، `app/admin/page.tsx`، `app/api/admin/settings/route.ts` (حذف `ad_slots_enabled`)، `proxy.ts` (CSRF_EXEMPT برای بیکن)، `vercel.json` (کران دوم)
* **حذف:** `apps/web/lib/ads/slots.ts`، `apps/web/app/api/admin/ad-slots/route.ts`

## Database Changes (فاز ۲)

* ۶ جایگاه seed شده، **همه `is_active=false`** (رفتار فعلیِ بدون بنر حفظ شد؛ روشن‌کردن با پنل ادمین). حالت پیش‌فرض: سه جایگاه موجودیتی `manual`، سه جایگاه بنری `paid`. ظرفیت‌ها: ۱۴/۸/۱۲/۱/۱/۱.
* ۵ پلن پیش‌فرض قیمت‌گذاری (تومان، در DB و قابل ویرایش از پنل — هاردکد نیست): رایگان ۰؛ پایه ۲۴۹٬۰۰۰/۳ آگهی/۳۰ روز؛ حرفه‌ای ۴۹۹٬۰۰۰/۷/۶۰؛ کسب‌وکار ۹۹۹٬۰۰۰/۱۵/۶۰؛ فروشگاهی ۱٬۹۹۰٬۰۰۰/۳۰/۹۰.
* جدول‌های قدیمی `ad_slots`/`ad_placements` **دست‌نخورده به‌عنوان آرشیو** ماندند؛ `ad_requests` همچنان زنده و متصل است. کلید `ad_slots_enabled` در app_settings مانده ولی هیچ کدی دیگر نمی‌خواندش.

## Migration (فاز ۲)

* کپی دادهٔ جایگاه‌ها/بنرهای قدیمی با نگاشت `market_1→equipment_ads_right`، `market_2→equipment_ads_left`، `footer→homepage_bottom_banner`.
* **Idempotent**: statementهای کپی داده با مارکر `mig_015_data_done` در app_settings گیت شده‌اند — اجرای مجدد فایل، ویرایش‌های ادمین را بازنویسی یا کمپین حذف‌شده را زنده نمی‌کند.
* اعتبارها (ad_credits) و سفارش‌ها (campaign_orders) فقط اسکیما هستند؛ فلوهای خرید در فازهای ۴–۵ تکمیل می‌شوند (سهمیهٔ رایگان و اعتبار پولی مستقل‌اند).

## Tests (فاز ۲)

**۱۹/۱۹ پاس** (اسکریپت `phase2-tests.js` علیه بیلد production محلی + دیتابیس زنده — بعد از رفع یافته‌های بازبینی دوباره اجرا شد): ساخت ۶ جایگاه، شروع inactive، ذخیرهٔ مستقل free/manual/paid، رد mode نامعتبر توسط CHECK، ساخت کمپین بنری، رد پنجرهٔ زمانی معکوس، غیبت جایگاه inactive از خروجی عمومی، **رندر جایگاه active با وجود `ad_slots_enabled=false` (اثبات حذف Master Toggle)**، صحت محتوای بنر، آداپتور legacy با کلید `market_1`، پذیرش هر ۸ وضعیت، رد وضعیت خارج از لیست (PAUSED)، انقضای خودکار ACTIVE→EXPIRED، فعال‌سازی SCHEDULED→ACTIVE، resolve کمپین موجودیتی به snapshot محصول، ۵ پلن با اعداد دقیق spec، ویرایش‌پذیری قیمت پلن. `npx tsc --noEmit` و `npm run build` سبز.

## Known Issues (فاز ۲)

* کارت‌های باشگاه/فروشندهٔ placement-driven مثل کارت‌های هاردکد فعلی، rating/تعداد میز نمایشی دارند (اسکیمای واقعی این فیلدها را ندارد) — تعیین تکلیف در فاز ۶.
* بیکن شمارش بدون auth است (مثل سیستم قبلی)؛ mitigation فعلی: فقط کمپین ACTIVE داخل پنجره شمرده می‌شود و شمارش اتمیک است. سخت‌گیری برای صورتحساب (dedupe/viewport/rate-limit) = فاز ۶.
* کمپین DRAFT هرگز خودکار منقضی نمی‌شود (عمدی — پیش‌نویس مالِ کاربر است).
* باندل‌های کش‌شدهٔ پنل ادمین تا refresh ممکن است روی `/api/admin/ad-slots` حذف‌شده 404 ببینند (یک‌بار refresh حل می‌کند).
* `href="\plans"` با بک‌اسلش در `shop/new` (یافتهٔ ممیزی فاز ۱ — خارج از دامنهٔ فاز ۲، در فاز پاک‌سازی اصلاح شود).

## Next Phase

PHASE 3 — IDENTITY & VERIFIED ROLES (فقط با دستور صریح مالک شروع می‌شود؛ تصمیم D2 دربارهٔ دو حساب تستی عیناً رعایت خواهد شد) ← سپس فاز ۴ سهمیهٔ v2، فاز ۵ پرداخت/رزرو جایگاه + ledger، فاز ۶ آنالیتیکس/پاک‌سازی.

---

# PHASE 1 — AUDIT (آرشیو)

COMPLETED — ۱۴۰۵/۰۵/۰۷ (2026-07-29)

هیچ تغییری در کد ایجاد نشده؛ فقط همین فایل ساخته شده است.
روش ممیزی: ۸ ممیزِ موازی روی زیرسیستم‌ها + یک منتقدِ کامل‌بودن که ادعاهای مشکوک را با خواندن دوبارهٔ کد راستی‌آزمایی کرد + اندازه‌گیری زندهٔ دیتابیس Supabase (فهرست جدول‌ها، شمارش ردیف‌ها، مقادیر واقعی app_settings).

---

## Completed

* بررسی کامل ساختار پروژه (frontend، backend مرده، دیتابیس، API، auth، نقش‌ها، هویت، ادمین، آگهی، تبلیغات، پرداخت، آپلود، آنالیتیکس)
* بررسی سیستم کاربران/نقش‌ها/کد ملی — با پاسخ دقیق به تک‌تک سؤالات فاز ۱
* بررسی سیستم آگهی (products) و موتور سهمیهٔ فعلی
* بررسی سیستم تبلیغات فعلی (ad_slots / ad_placements / ad_requests) + تعیین تکلیف حذف/Refactor/حفظ/Migration
* بررسی شش بخش صفحهٔ اصلی و منبع دادهٔ هرکدام
* بررسی لایهٔ پرداخت و حکم دربارهٔ بازاستفاده برای تبلیغات
* اندازه‌گیری زندهٔ دیتابیس: ۳۰ جدول، ۱۱ تابع bh_*، ۷ کلید app_settings، **۲ حساب با ۱ کد ملی مشترک**، مقدار ذخیره‌شدهٔ `ads_free_quota` هنوز شکل قدیمی `{quota:3, period:"week"}`
* طراحی معماری پیشنهادی (Person/Identity + Advertising) و برنامهٔ اجرایی مرحله‌ای
* ثبت دو تصمیم مالک دربارهٔ جای بنرهای کناری و حساب‌های تستی (بخش Decisions)

---

## Decisions

تصمیمات قطعی مالک — ۱۴۰۵/۰۵/۰۷. این دو، سؤال‌های بازِ انتهای ممیزی را می‌بندند و بر هر متن دیگری در این سند مقدم‌اند.

### D1 — جای دو بنر تبلیغاتی: سکشن «فروشگاه‌های تجهیزات»، نه «بیلیارد بازار»

دو Placement کناری (`equipment_ads_right` و `equipment_ads_left`) متعلق به سکشن **فروشگاه‌های تجهیزات** هستند. ساختار نهایی صفحهٔ اصلی:

**سکشن بیلیارد بازار**
* نمایش ۱۴ محصول — Placement: `market_featured_products_homepage`
* این سکشن **هیچ‌کدام از دو بنر کناری را ندارد** (دو `<AdSlot>` فعلی که انتهای این سکشن کار گذاشته شده‌اند، در فاز ۲ باید به سکشن فروشگاه‌ها منتقل شوند)

**سکشن فروشگاه‌های تجهیزات** (در Desktop به‌ترتیب راست / محتوای اصلی / چپ)
* تبلیغ اسلایدری سمت راست — `equipment_ads_right`
* نمایش ۱۲ فروشگاه تجهیزات — `featured_equipment_stores_homepage`
* تبلیغ اسلایدری سمت چپ — `equipment_ads_left`

قواعد:
* هر دو Placement تبلیغاتی کاملاً مستقل از `market_featured_products_homepage` هستند (فعال/غیرفعال/حالت مستقل — طبق اصل «بدون Master Toggle»).
* **Desktop:** چیدمان سه‌ستونه راست / اصلی / چپ.
* **Mobile:** کاملاً Responsive؛ بدون Overflow افقی و بدون به‌هم‌ریختن UI (ستون‌ها زیر هم می‌آیند؛ همان قاعدهٔ موجود پروژه: هیچ عنصری صفحه را عریض نکند).
* تبلیغ‌ها «اسلایدری» هستند — یعنی هر جایگاه کناری می‌تواند بیش از یک بنر بچرخاند (بر خلاف نمایش ثابتِ فعلی؛ نیازمند rotation در فاز ۲).

نگاشت مهاجرت داده (اصلاح‌شده):
* `market_1` → `equipment_ads_right`
* `market_2` → `equipment_ads_left`
* `footer` → `homepage_bottom_banner`
* جدید: `market_featured_products_homepage`، `featured_clubs_homepage`، `featured_equipment_stores_homepage`

### D2 — دو حساب موجود با کد ملی یکسان: حساب‌های تستی مالک — دست نزن

دو حسابی که در ممیزی با یک `national_id` مشترک پیدا شدند، **حساب‌های تستی خودِ مالک** هستند (عمداً با دو شمارهٔ موبایل برای تست بخش‌های مختلف سایت ساخته شده‌اند). بنابراین:

* هیچ‌کدام **حذف نشوند**؛ هیچ‌کدام با دیگری **Merge نشوند**؛ داده‌های هر دو حفظ شود.
* این دو حساب «دادهٔ تستی موجود» تلقی می‌شوند.
* **هیچ Merge یا حذف خودکاری در هیچ Migrationی انجام نشود** — هر اقدامی روی این دو حساب فقط با تأیید صریح مالک.

اصول معماری Production (بدون تغییر):
* هر شخص واقعی یک `Person / Identity` یکتا.
* یکتاییِ National ID در سطح Identity مدیریت می‌شود.
* چند User Account می‌توانند به یک Identity متصل باشند.
* Quota در سطح Person محاسبه می‌شود، نه User Account.

**راهکار امن و قابل‌برگشت برای Migration فاز ۳** (اعلام طبق درخواست — اجرا فقط پس از تأیید):
* قید یکتایی روی `persons.national_id_hash` است، نه روی `users.national_id` — پس وجود دو ردیف user با یک کد ملی **هیچ Conflictی با قید ایجاد نمی‌کند**: یک ردیف `persons` ساخته می‌شود و هر دو حساب با `users.person_id` به همان یک ردیف اشاره می‌کنند. این «اتصال» است، نه Merge: هر دو حساب، نشست‌ها، آگهی‌ها و پروفایل‌هایشان دست‌نخورده و مستقل می‌مانند و برگشت‌پذیر است (فقط یک مقدار FK است که می‌توان NULL کرد).
* پیامد جانبی که باید پیش از فاز ۴ تأیید شود: با «سهمیه در سطح Person»، این دو حساب تستی سهمیهٔ مشترک خواهند داشت (مطابق قانون Production). اگر برای تست مزاحم است، گزینهٔ جایگزین: پرچم `is_test` روی ردیف person این دو حساب که فقط سهمیهٔ مشترک را برایشان معلق کند — تصمیمش با مالک در فاز ۴.
* بک‌فیل بقیهٔ کاربران (که کد ملی یکتا دارند) بدون ابهام است.

---

## Findings

### هویت و نقش‌ها (بحرانی‌ترین یافته)

* **سیستم نقش‌ها عملاً مرده است.** صفحهٔ انتخاب نقش (`app/profile/role/page.tsx`) درخواست را به `${API}/roles/request` می‌فرستد که با `NEXT_PUBLIC_API_URL=/api` به مسیر ناموجود می‌خورد (۴۰۴ بی‌صدا)؛ سپس نقش صرفاً با `updateUser` در **localStorage خود کاربر** «فعال» می‌شود. صفحهٔ ادمین نقش‌ها (`app/admin/roles/page.tsx`) به `/api/admin/roles` وصل است که **وجود ندارد** (همیشه «سرویس در دسترس نیست»). جدول `role_requests` در دیتابیس هست (ساختهٔ TypeORM مرده) ولی خالی و بی‌استفاده (۰ ردیف).
* **`PATCH /api/users/me` به هر کاربر اجازه می‌دهد `primaryRole` خودش را آزادانه عوض کند** (فقط با whitelist نام نقش‌ها). ترکیب این با سهمیهٔ نقش‌محور یعنی هر کاربر می‌تواند خودش را «فروشنده» کند و سهمیهٔ بیشتر بگیرد.
* **Person/Identity وجود ندارد.** سهمیه به `user.id` گره خورده. `national_id` هیچ قید یکتایی ندارد؛ در دیتابیس زنده **۲ حساب یک کد ملی مشترک دارند** (تست‌های عمدی مالک — طبق تصمیم قبلی «فعلاً اعمال نشود»).
* احراز هویت ثبت‌نام (OTP + شاهکار + ثبت‌احوال) سرورساید و نسبتاً محکم است؛ نشان‌های `verifiedAt`/`idHash` سمت سرور نگه داشته می‌شوند و کلاینت نمی‌تواند کد ملیِ تأییدشده جعل کند. دو گریزگاه: بدون `SMS_API_KEY` هر دو استعلام `match:true` برمی‌گردانند، و اگر PersonInfo در دسترس نباشد حساب با نامِ تطبیق‌نخورده verified می‌شود (فلگ `nameChecked:false` هیچ‌جا ذخیره نمی‌شود).
* جدول `users` در **هیچ مایگریشنی ساخته نمی‌شود** — تنها سند اسکیمایش entity مردهٔ NestJS است (`apps/api/src/modules/user/user.entity.ts`) که آن هم کامل نیست (مثلاً `club_id` را ندارد).

### آگهی و سهمیه

* آگهی = ردیف `products` با `sellerId = user.id`؛ POST/PATCH/DELETE در `/api/market/ads` با مالکیت درست.
* **هیچ انقضایی برای آگهی نیست.** ستون `expiresAt` در مایگریشن ۰۰۶ ساخته شده ولی هیچ کدی آن را نمی‌خواند/نمی‌نویسد؛ آگهیِ active برای همیشه می‌ماند.
* **حذف آگهی حذف فیزیکی است و سهمیه را در همان دوره برمی‌گرداند** (شمارش = count ردیف‌های موجود با `createdAt` در پنجره) — ناقض قانون فاز ۱. چرخهٔ «ثبت→حذف→ثبت» بی‌نهایت ممکن است.
* موتور سهمیه (`lib/ads/quota.ts`): پنجرهٔ لغزان day/week/month؛ **نقشِ صرفاً حاضر (نه تأییدشده) شمرده می‌شود**؛ سخاوتمندانه‌ترین نقش برنده (همین قانون درست است)؛ `limit<=0` = نامحدود؛ خطای دیتابیس ⇒ fail-open؛ چکِ سهمیه و INSERT غیراتمیک (race در درخواست‌های همزمان).
* **مقدار زندهٔ `ads_free_quota` هنوز شکل قدیمی `{quota:3,"week"}` است** ⇒ شاخهٔ legacy در `normalizeFreeQuota` آن را «۳ در هفته برای همهٔ نقش‌ها» بسط می‌دهد؛ اعداد per-role کد تا وقتی ادمین از پنل ذخیره نکند بی‌اثرند.
* زنجیرهٔ فروش بسته (۰۰۷: `ad_plans`/`ad_plan_orders`/`user_ad_plans` + `bh_activate_ad_plan`) سالم است: قیمت از سرور، snapshot ضد تغییر قیمت، callback ایدمپوتنت، فعال‌سازی اتمیک. آینهٔ استوری (۰۱۳) هم همین‌طور.
* `status` محصولات ناسازگار: enum قدیمی `active|sold|inactive`، POST همیشه `active`، PATCH فقط `active|paused`، چک `deleted` کد مرده.
* باگ ریز: لینک باکس سهمیه در `shop/new` با بک‌اسلش `href="\plans"` نوشته شده.

### تبلیغات (بنر) فعلی

* سه جایگاه (`market_1`، `market_2`، `footer`) + `ad_placements` (بنر ادمین‌ساخته) + `ad_requests` (فرم تماس). **هیچ فلوی پرداخت/رزروی برای جایگاه نیست** — قیمت فقط نمایشی؛ فلوی تجاری = تماس دستی.
* **کلید سراسری `ad_slots_enabled` همهٔ جایگاه‌ها را با هم خاموش/روشن می‌کند** — دقیقاً همان Master Toggle که معماری هدف ممنوع کرده.
* هیچ rotation با capacity>1، هیچ cron انقضا (بنر منقضی در پنل «ACTIVE» می‌ماند و فقط با فیلتر خواندن حذف می‌شود).
* شمارش impression/click: بدون auth، غیراتمیک (read-then-update)، بدون چک دیده‌شدن واقعی — **برای صورتحساب قابل اتکا نیست**.
* Legacy: `app/admin/ads` فقط redirect است؛ هیچ سیستم Campaign/Banner قدیمی دیگری نمانده. `budget` در فرم advertise حذف شده ولی API/ستون هنوز می‌پذیرند (همیشه NULL). متن پنل ادمین هنوز «چهار جایگاه» می‌گوید (سه‌تاست).

### صفحهٔ اصلی

* هر سه سکشن «ویژه» از **آرایه‌های هاردکد داخل `app/page.tsx`** رندر می‌شوند: `PRODUCTS` (۱۴)، `CLUBS` (۸)، `SELLERS` (۱۲) — هیچ fetchی در کار نیست و با دادهٔ صفحات اصلی خودشان ناسازگارند (کلیک روی «باشگاه ستاره تهران» id=1 به صفحهٔ «باشگاه سنچوری» می‌رود!).
* AdSlotهای واقعی: `market_1`/`market_2` انتهای سکشن بازار (`page.tsx:1748-1749`) و `footer` در `layout.tsx:84`.
* ۴ بنر دکوری هاردکد (`MktBanner`) زیر بازار و زیر فروشندگان + اسلایدر `BANNER_SLIDES` با محتوای تاریخ‌دار کهنه‌شونده («تابستان ۱۴۰۴»).
* منابع دادهٔ واقعیِ آماده برای placement-driven شدن: `GET /api/clubs` (جدول clubs)، `GET /api/market/ads` (products)، `fetchProfiles('seller')` (جدول profiles).

### پرداخت

* قرارداد Provider-agnostic (`lib/payments`) با mock + zarinpal؛ الگوی «سفارش+snapshot → درگاه → callback با verify سروری و مقایسهٔ مبلغ → فعال‌سازی اتمیک ایدمپوتنت» دو بار (آگهی/استوری) اثبات شده — **برای رزرو جایگاه قابل بازاستفاده است**.
* کمبودها: refund زرین‌پال stub است (فقط دستی)؛ `getPaymentStatus` پیاده نشده (reconciliation سفارش‌های PENDING رهاشده ممکن نیست)؛ **خرید بسته‌ها هیچ ردیفی در `ledger_entries` نمی‌سازد ⇒ درآمد تبلیغات از داشبورد مالی ادمین نامرئی است**؛ `platform_bank`/`story_platform_bank` فقط متادیتای نمایشی‌اند؛ ایندکس `provider_authority` در جدول‌های سفارش پلن یکتا نیست (payments یکتاست)؛ REFUNDED در CHECK سفارش‌ها نیست.
* باگ: retry پرداخت رزرو بعد از FAILED با برخورد `idempotency_key` یکتا ⇒ ۵۰۰ دائمی.
* **تله برای فاز بعد: `/api/ads/plans/callback` و `/api/stories/plans/callback` در `CSRF_EXEMPT` نیستند** (فقط `/api/payments/callback` هست) — با زرین‌پالِ GET-محور کار می‌کند ولی هر PSP با کالبک POST مرورگری ۴۰۳ می‌گیرد.
* درگاه فعال از روی ریپو قابل تعیین نیست (env پروداکشن در Vercel است)؛ در env محلی mock است.

### زیرساخت و بدهی فنی مرتبط

* پنل ادمین دو دنیاست: مالی/رزرو/تبلیغات server-backed با گارد ۳لایه (proxy + isAdmin + کلاینت)؛ ولی تأیید پروفایل‌ها (مربی/داور/فروشنده/بازیکن/…)، رنکینگ و مدیا فقط **localStorage خود ادمین**؛ news/events حتی localStorage هم نیستند (state حافظه‌ای). سه صفحهٔ ادمین با axios به NestJS مرده می‌زنند (همیشه خالی). `/api/admin/profiles` سرورساید ساخته شده ولی **هیچ صفحهٔ ادمینی صدایش نمی‌زند**.
* پس‌درِ هاردکد: `ADMIN_PHONE='09121327283'` در سه صفحهٔ ادمین (کلاینتی).
* عکس آگهی‌های بازار **به‌صورت base64 داخل ستون `products.images`** ذخیره می‌شود (تا ۵×۵MB بدون فشرده‌سازی و بدون سقف سروری) — خطر سقف بدنهٔ ~4.5MB در Vercel و پاسخ‌های سنگین.
* زیرساخت cron موجود است (`vercel.json` → `/api/cron/expire-bookings` روزانه) ولی **هیچ jobی برای انقضای بنر/بسته نیست**؛ endpoint کران بدون `CRON_SECRET` برای عموم باز است.
* دیتابیس: **هیچ FK به users یا clubs در کل مایگریشن‌ها نیست**؛ سه رژیم نام‌گذاری (camelCase قدیمی/snake_case جدید/دورگه)؛ RLS روشن با صفر policy (مدل service-role-only — عمدی)؛ جداول پایه خارج از version control.
* شمارنده‌ها/استوری/OTP روی باکت **عمومی** `club-media` به‌صورت JSON — رکورد OTP با نام فایل = شماره موبایل قابل خواندن عمومی است؛ POST/DELETE استوری باشگاه/فروشگاه **هیچ auth ندارد**.

---

## Existing Architecture

```
کاربر (users) ── primaryRole/secondaryRoles (خوداظهاری، بدون تأیید سرورساید)
   │                      ▲ PATCH /api/users/me (آزاد!)
   │ national_id (بدون یکتایی؛ Person وجود ندارد)
   │
   ├─ آگهی: products(sellerId) ←گیت← lib/ads/quota (پنجرهٔ لغزان، نقشِ حاضر، حذف=برگشت سهمیه)
   │        └─ بسته: ad_plans → ad_plan_orders → user_ad_plans (bh_activate_ad_plan، بدون ledger)
   ├─ استوری: آینهٔ کامل (story_*, bh_activate_story_plan) + شمارش از JSON در Storage
   └─ بنر:   ad_slots(3) → ad_placements (ادمین‌دستی، بدون پرداخت/rotation/انقضا)
              └─ Master Toggle: app_settings.ad_slots_enabled  ← باید حذف شود
پرداخت: lib/payments (mock|zarinpal) — الگوی callback+activation اثبات‌شده
صفحهٔ اصلی: سه سکشن ویژه = آرایه‌های هاردکد در page.tsx (بدون هیچ اتصال به داده)
```

## Proposed Architecture

### لایهٔ هویت

```
persons (شخص حقیقی)
  id uuid PK
  national_id_hash text UNIQUE   ← کلید یکتایی؛ HMAC مثل الگوی موجود otp-server
  created_at
    ▲ users.person_id FK  (بک‌فیل: هر national_id_verified موجود؛ دو حسابِ فعلیِ هم‌کدملی → یک person)
    ▲ role_grants (جایگزین سیستم مردهٔ نقش‌ها)
        person_id FK, role, status(pending|approved|rejected),
        doc_url, reviewed_by, reviewed_at, note
        UNIQUE(person_id, role)
```

* گرنت نقش **فقط سرورساید** پس از تأیید ادمین؛ `PATCH /api/users/me` برای primaryRole بسته می‌شود؛ `updateUser` کلاینتی دیگر منبع نقش نیست.
* ادمین: صفحهٔ واقعی بررسی درخواست نقش (به‌جای `/api/admin/roles` ناموجود) + اتصال `/api/admin/profiles` موجود.

### لایهٔ سهمیهٔ رایگان (قوانین بخش ۸)

```
quota_consumptions (دفترِ مصرف — نه شمارش ردیف‌های زنده)
  id, person_id FK, kind('ad'), ref_id(آگهی، nullable), consumed_at
```

* Free Quota = **max** سهمیه بین نقش‌های **approved** شخص (جمع نمی‌شوند) — تابع `bh_consume_quota(person_id)` اتمیک با FOR UPDATE (رفع race فعلی).
* دوره = پنجرهٔ لغزان ۳۰ روزه روی `consumed_at`؛ **حذف/انقضای آگهی ردیف مصرف را پاک نمی‌کند** ⇒ سهمیه در همان دوره برنمی‌گردد (قانون ۸ برقرار).
* اعداد: user/player/referee=۱، technician/coach=۲، club_owner/manufacturer/seller=۴ — در `app_settings.ads_free_quota` با شکل per-role (مهاجرت مقدار legacy فعلی الزامی).
* سهمیه و شمارش **به person** گره می‌خورد نه user ⇒ چند حساب با یک کد ملی سهمیهٔ جدا نمی‌گیرند.
* بستهٔ خریداری‌شده (user_ad_plans) روی سهمیهٔ رایگان سوار می‌شود (بدون تغییر ساختاری).

### لایهٔ تبلیغات

```
placements (تعمیم ad_slots — ۶ جایگاهِ مستقل، بدون Master Toggle)
  key PK: market_featured_products_homepage │ featured_clubs_homepage │
          featured_equipment_stores_homepage │ equipment_ads_right │
          equipment_ads_left │ homepage_bottom_banner
  is_active bool (مستقل)  mode('free'|'manual'|'paid')
  content_kind('banner'|'entity')  price, capacity, duration_days, sort_order

campaigns (تعمیم ad_placements)
  id, placement_key FK, user_id/person_id, advertiser,
  content jsonb: بنر {image_url, link_url} یا موجودیت {type:'product'|'club'|'seller', ref}
  status(DRAFT|PENDING_PAYMENT|ACTIVE|PAUSED|EXPIRED), starts_at, ends_at, weight

placement_orders (خرید جایگاه — الگوی اثبات‌شدهٔ ad_plan_orders)
  + bh_activate_campaign: اتمیک، ایدمپوتنت، چکِ ظرفیتِ جایگاه با FOR UPDATE
  + ثبت در ledger_entries (type جدید AD_REVENUE) → دیده‌شدن در داشبورد مالی

campaign_stats / rpc bh_track(campaign_id, kind): شمارش اتمیک (update ... = x+1)
cron روزانه: انقضای campaigns و user_*_plans (status→EXPIRED) با CRON_SECRET اجباری
```

* سه سکشن «ویژه» صفحهٔ اصلی از هاردکد به campaigns با `content_kind='entity'` مهاجرت می‌کنند (fallback به آرایه‌های فعلی تا پرشدن)؛ دو بنر کناری و بنر فوتر = مهاجرت دادهٔ سه جایگاه فعلی (`market_1→equipment_ads_right`، `market_2→equipment_ads_left`، `footer→homepage_bottom_banner`).
* اتصال دو سیستم: خرید جایگاه/بسته ⇒ `person_id` از نشست؛ ادمین در پنل، درخواست تبلیغ (ad_requests موجود) را به campaign تبدیل می‌کند.

---

## Risks

* **قفل شدن کاربران هنگام سخت‌گیری نقش‌ها**: امروز نقش‌ها localStorage‌اند؛ با انتقال به role_grants سرورساید، نقش‌های فعلی کاربران باید بک‌فیل یا مسیر باز-درخواست داده شود وگرنه پنل‌هایشان بسته می‌شود.
* **دو حسابِ هم‌کدملیِ موجود** (تستی مالک): طبق تصمیم D2 هیچ Merge/حذف خودکاری ممنوع؛ راهکار اتصالِ برگشت‌پذیر هر دو به یک ردیف person در همان بخش اعلام شده و فقط با تأیید مالک اجرا می‌شود.
* env پروداکشن (Vercel) از ریپو قابل دیدن نیست — حکم دربارهٔ درگاه فعال/SMS/CRON_SECRET قبل از فاز ۵ باید با خروجی env واقعی سنجیده شود.
* تغییر کلید جایگاه‌ها (market_1→equipment_ads_right و …) مهاجرت داده می‌خواهد و `SLOT_KEYS` هاردکد کلاینت باید هم‌زمان عوض شود (ناسازگاری نسخهٔ کش‌شدهٔ کلاینت در لحظهٔ دیپلوی).
* حذف Master Toggle یعنی `ad_slots_enabled` فعلی باید به `is_active` تک‌تک جایگاه‌ها ترجمه شود (الان false است ⇒ همهٔ جایگاه‌ها inactive شروع می‌کنند — رفتار فعلی حفظ می‌شود).
* شمارنده‌های فعلی برای صورتحساب قابل اتکا نیستند؛ اگر قیمت‌گذاری CPM/CPC خواسته شود، سخت‌گیری (dedupe، viewport، rate-limit) پیش‌نیاز است.
* جدول‌های پایه (users/products/clubs) خارج از مایگریشن‌اند — هر ALTER جدید باید با احتیاط و idempotent نوشته شود؛ اسکیمای پایه باید ابتدا dump و در ریپو مستند شود.
* حجم base64 در `products.images` می‌تواند در حین مهاجرت به Storage باعث درخواست‌های سنگین/timeout شود — مهاجرت باید تدریجی باشد.
* قانون «انتخاب نقش به‌تنهایی سهمیه نمی‌سازد» رفتار امروز را برای کاربران دارای نقشِ تأییدنشده سخت‌تر می‌کند — نیاز به پیام روشن در UI.

## Files Reviewed

(نمایندهٔ ~۱۲۰ فایل خوانده‌شده؛ کامل‌ها در گزارش ممیزها)

* `apps/web/app/api/auth/register/route.ts`، `app/register/page.tsx`، `lib/otp-server.ts`، `lib/shahkar-server.ts`، `app/api/shahkar/route.ts`، `app/api/users/me/route.ts`، `app/api/users/profile/route.ts`، `app/api/auth/change-phone/route.ts`، `app/profile/role/page.tsx`، `lib/roles.ts`، `app/admin/roles/page.tsx`، `store/auth.store.ts`
* `apps/web/app/api/market/ads/route.ts` + `[id]/route.ts`، `lib/ads/quota.ts`، `lib/ads/plans.ts`، `app/api/ads/plans/*`، `app/api/admin/ad-plans/route.ts`، `app/shop/new/page.tsx`، `app/plans/page.tsx`، `components/ads/MyAdPlan.tsx`
* `apps/web/lib/ads/slots.ts`، `components/ads/AdSlot.tsx`، `app/api/ads/slots/route.ts`، `app/api/ads/requests/route.ts`، `app/api/admin/ad-slots/route.ts`، `app/admin/ad-slots/page.tsx`، `app/advertise/page.tsx`
* `apps/web/lib/payments/{provider,index,mock,zarinpal}.ts`، `app/api/payments/create` + `callback/[provider]`، `app/api/bookings/[id]/cancel/route.ts`، `lib/finance/db.ts`
* `apps/web/app/page.tsx` (۲۰۱۴ خط)، `app/layout.tsx`، `proxy.ts`، `vercel.json`، `app/api/cron/expire-bookings/route.ts`
* `apps/web/app/admin/*` (هر ۲۴ مسیر)، `app/api/admin/settings/route.ts`، `app/api/admin/profiles/route.ts`
* `supabase/migrations/001..014` (همه)، `apps/api/src/modules/{user,product,role}/*.entity.ts` (اسناد اسکیمای پایه)
* `apps/web/lib/{social-server,stories/*,profiles/*,supabase,supabase-server,notify,sms-server,push-server,email-server,bank-server,license-server}.ts`

## Files That Will Change

(پیش‌بینی فازهای ۲ به بعد — در فاز ۱ هیچ‌کدام تغییر نکرده‌اند)

* **فاز ۲ (هستهٔ تبلیغات):** `lib/ads/slots.ts` (بازنویسی به placements/campaigns)، `components/ads/AdSlot.tsx`، `app/api/ads/slots/route.ts`، `app/api/admin/ad-slots/route.ts`، `app/admin/ad-slots/page.tsx`، `app/page.tsx` (سه سکشن ویژه + دو AdSlot)، `app/layout.tsx`، `app/advertise/page.tsx`، `app/api/admin/settings/route.ts` (حذف ad_slots_enabled از whitelist)
* **فاز ۳ (هویت/نقش):** `app/profile/role/page.tsx`، `app/admin/roles/page.tsx` + route جدید `app/api/roles/*` و `app/api/admin/roles/*`، `app/api/users/me/route.ts` (بستن تغییر آزاد نقش)، `app/api/auth/register/route.ts` (اتصال person)، `store/auth.store.ts`
* **فاز ۴ (سهمیه v2):** `lib/ads/quota.ts`، `app/api/market/ads/route.ts` (مصرف اتمیک + انقضا)، `lib/stories/quota.ts`، `app/api/ads/quota/route.ts`، `components/ads/MyAdPlan.tsx`، `app/admin/ad-plans/page.tsx` (اعداد جدید)
* **فاز ۵ (پرداخت جایگاه):** `lib/payments/*` (getPaymentStatus)، route جدید placement_orders + callback، `proxy.ts` (CSRF_EXEMPT)، `app/api/admin/finance/route.ts` (ledger تبلیغات)، `vercel.json` + cron جدید
* **فاز ۶ (آنالیتیکس/پاک‌سازی):** شمارش اتمیک، offload تصاویر products، حذف کد مرده (DiscoveryPanel/NEWS/mkt-dots)، تعیین تکلیف `apps/api`

## Database Changes Required

1. `persons` + `users.person_id` (FK، بک‌فیل از national_id موجود با هش HMAC)
2. `role_grants` (جایگزین role_requests مرده — یا بازسازی همان جدول با اسکیمای وب)
3. `quota_consumptions` + تابع اتمیک `bh_consume_quota`
4. `placements` (مهاجرت/rename از `ad_slots` با ۶ کلید جدید + ستون‌های mode/content_kind) — حذف کلید `ad_slots_enabled` از app_settings
5. `campaigns` (مهاجرت از `ad_placements`) + `placement_orders` + `bh_activate_campaign` (با enforcement ظرفیت در DB)
6. `ledger_entries`: type جدید `AD_REVENUE` (+ ثبت برای فروش بسته‌های موجود)
7. به‌روزرسانی مقدار `ads_free_quota` به شکل per-role با اعداد بخش ۸ (۳۰روزه)
8. ایندکس‌های یکتا روی `provider_authority` سفارش‌ها؛ `status='EXPIRED'` خودکار با cron
9. مستندسازی اسکیمای جداول پایه (dump ساختار users/products/clubs/bookings در ریپو)
10. (اختیاری، توصیه‌شده) FKهای گمشده به users/clubs — حداقل برای جدول‌های جدید

## Next Phase (ثبت‌شده در پایان فاز ۱ — انجام شد)

PHASE 2 — CORE ADVERTISING SYSTEM

پیشنهاد ترتیب: ۲) هستهٔ Placement/Campaign مستقل و مهاجرت سه جایگاه فعلی ← ۳) Person/Identity و نقش‌های تأییدشده ← ۴) سهمیهٔ v2 (چون به نقش تأییدشده وابسته است) ← ۵) پرداخت/رزرو جایگاه + ledger + cron ← ۶) آنالیتیکس و پاک‌سازی.

هر دو سؤالِ باز ممیزی با تصمیمات D1 و D2 (بخش Decisions) بسته شدند: بنرهای کناری به سکشن «فروشگاه‌های تجهیزات» می‌روند (راست/اصلی/چپ در Desktop، Responsive بدون Overflow در Mobile)، و دو حساب تستیِ هم‌کدملی بدون هیچ Merge/حذفی به‌عنوان دادهٔ تستی می‌مانند.

---

# PHASE 8 — آماده‌سازی نهایی برای Production

تاریخ: ۲۹ جولای ۲۰۲۶ (۷ مرداد ۱۴۰۵)
وضعیت: **انجام شد — ولی حکم نهایی NO-GO است** (به بخش «حکم» نگاه کنید)

## آنچه ساخته شد

### ۱) بازیابی رمز عبور (Forgot Password)
روی همان زیرساخت OTP موجود، بدون هیچ سیستم احراز هویت موازی.

* `app/api/auth/forgot-password/route.ts` — سه گام `send` / `verify` / `reset`
  - **ضد شمارش کاربر:** پاسخ گام اول همیشه یکسان است؛ برای شمارهٔ ناموجود هیچ پیامکی نمی‌رود.
  - **هیچ توکنی در URL یا پاسخ نیست** — اثبات تأیید همان رکورد سرورساید OTP است.
  - رمز تازه نباید همان رمز فعلی باشد (bcrypt.compare).
  - پس از موفقیت: `revokeAllSessions(uid, 'password_reset')` + `audit(PASSWORD_RESET)`.
* `app/forgot-password/page.tsx` — هر سه گام در یک صفحه، با همان ظاهر صفحهٔ ورود
  (کارت ۴۲۰px، نوار طلایی بالا، خطای مرکزی، نشانگر پله‌ها، شمارش معکوس ارسال دوباره،
  چک‌لیست زندهٔ شرایط رمز).
* لینک «فراموشی رمز عبور؟» کنار برچسب رمز در `app/login/page.tsx`.

### ۲) محافظت در برابر حدس رمز (Brute Force)
* `supabase/migrations/023_login_protection.sql` — **اجرا شد روی دیتابیس**
  - جدول `login_attempts` با `UNIQUE (scope, subject)`؛ دو دامنهٔ جدا: `account` و `ip`.
  - `bh_login_guard` (فقط خواندن) / `bh_login_fail` (اتمیک با `FOR UPDATE`) / `bh_login_ok`.
  - قفل **تدریجی و موقت**: ۵ دقیقه ← ۳۰ دقیقه ← ۱ ساعت ← ۴ ساعت. هرگز دائمی نمی‌شود،
    چون قفل دائمی خودش سلاح DoS علیه کاربر است (طبق خواستهٔ صریح).
  - هر سه تابع + خود جدول از `anon`/`authenticated` REVOKE شده‌اند.
* `lib/auth/login-guard.ts` — آستانه و پله‌ها با `LOGIN_FAIL_THRESHOLD` و `LOGIN_LOCK_WINDOWS`
  قابل تنظیم‌اند؛ در خطای زیرساخت عمداً **fail-open** است تا ورود همه بسته نشود.
* `app/api/auth/login/route.ts` — گارد + پیام یکسان `GENERIC_LOGIN_ERROR` برای
  «شماره نیست» و «رمز غلط» (بستن شمارش کاربر).

### ۳) تغییر رمز عبور
* `app/api/auth/change-password/route.ts` — `GET` فقط `{hasPassword}` برمی‌گرداند (هرگز خود هش).
  `POST` رمز فعلی را می‌سنجد؛ برای حساب‌های ساخته‌شده با OTP حالت «تعیین رمز» دارد.
  پس از موفقیت `revokeAllSessions` + `clearSessionCookies` + `audit(PASSWORD_CHANGED)`.
* `components/auth/ChangePassword.tsx` + بخش «رمز عبور» در `app/profile/me/page.tsx`.
* `lib/auth/password.ts` — منبع واحد سیاست رمز (۸ تا ۷۲ بایت، چهار دستهٔ نویسه، فهرست رمزهای رایج).

### ۴) اصلاح خطاها و نشتِ پیام دیتابیس
* `/api/products/[id]` — شناسهٔ غیر UUID دیگر ۵۰۰ با متن خام `invalid input syntax for
  type uuid` نمی‌دهد؛ حالا ۴۰۴ تمیز است.
* **۱۱ مسیر دیگر** که `error.message` خام را به کاربر برمی‌گرداندند اصلاح شدند
  (`users/by-role`, `clubs`, `clubs/my-clubs`, `clubs/[id]` ×۲, `clubs/[id]/tables`,
  `clubs/[id]/tables/sync`, `bookings`, `bookings/[id]/cancel`, `admin/bookings`, `products` ×۲).
  متن خام حالا فقط در `console.error` سرور می‌ماند.
* `users/by-role` — نقش مستقیم داخل رشتهٔ فیلتر PostgREST می‌نشست؛ با فهرست بستهٔ نقش‌ها
  بسته شد. `excludeId` هم با UUID-guard محافظت شد.

### ۵) هدرهای امنیتی و صفحه‌های خطا
* `next.config.js` — `Strict-Transport-Security`، `X-Frame-Options`، `X-Content-Type-Options`،
  `Referrer-Policy`، `Permissions-Policy`. **عمداً بدون CSP** (نیاز به report-only و تست کامل).
* `app/robots.ts` (بخش‌های خصوصی Disallow)، `app/sitemap.ts` (۲۰ مسیر عمومی)،
  `app/not-found.tsx`، `app/error.tsx` — هیچ‌کدام قبلاً وجود نداشتند.

### ۶) محدودیت نرخ روی استعلام شاهکار
`/api/shahkar` بدون هیچ سقفی باز بود: هم اعتبار سرویس پولی را می‌سوزاند، هم اوراکل
«این کد ملی مال این شماره هست؟» بود. حالا `RULES.shahkar` (۵ در ۱۵ دقیقه) + اعتبارسنجی شکل.

### ۷) آپلود — بستن سوراخ SVG
`lib/profiles/server.ts` هر `image/*` را می‌پذیرفت، از جمله `image/svg+xml`، و با همان
contentType در باکت عمومی می‌گذاشت. حالا نوع از روی **بایت‌های واقعی** (magic bytes) تشخیص
داده می‌شود و فقط jpeg/png/gif/webp/avif می‌گذرند.

## آزمون‌ها

`login_attempts` — ۱۴ آزمون، همه قبول (روی توابع واقعی دیتابیس، با شمارهٔ ساختگی
`09000000000` و IP آزمایشی `203.0.113.77` از بازهٔ TEST-NET-3؛ همهٔ ردیف‌ها پاک شدند):
گارد اولیه باز · زیر آستانه باز · آستانه ⇒ قفل ۳۰۰s · تشدید ⇒ ۱۸۰۰s · سقف ۴ ساعت حتی
پس از ۱۰ دور · قفل دائمی نمی‌شود · `bh_login_ok` شمارندهٔ حساب را پاک می‌کند و شمارندهٔ IP
را دست‌نخورده می‌گذارد · هر سه تابع و جدول برای `anon` بسته‌اند.

`npx tsc --noEmit` → پاک. `npm run build` → موفق.

## قیدهایی که رعایت شدند

`ads_quota_enabled` همچنان `false` · `PAYMENT_PROVIDER` و `ZARINPAL_MERCHANT_ID` دست‌نخورده ·
هیچ Force Push · هیچ تغییر مخرب روی دادهٔ Production · هیچ حساب/کاربری حذف یا Merge نشد ·
هیچ Secret در کد/گزارش/Git نوشته یا نمایش داده نشد · **هیچ Push و Deploy انجام نشد**.

## یافته‌های امنیتی — به‌ترتیب شدت

### 🔴 S1 (CRITICAL) — کل باکت Storage عمومی است و دادهٔ خصوصی در آن نشسته

باکت `club-media` تنها باکت پروژه است و `public: true` دارد، با
`file_size_limit: null` و `allowed_mime_types: null`. کلید `anon` می‌تواند **کل باکت را
فهرست کند**، و مسیر `/object/public/...` **بدون هیچ کلیدی** خواندنی است.

آنچه با آزمون فقط-خواندنی تأیید شد (محتوای هیچ فایلی چاپ یا ذخیره نشد — فقط کد وضعیت و اندازه):

| مسیر | چیست | تعداد | خواندنی بدون کلید؟ |
|---|---|---|---|
| `documents/referee/` | مدرک هویتی داوران (JPEG ~۵۰۰KB) | ۱ | ✅ ۲۰۰ |
| `social/otp/<شماره>.json` | رکورد OTP، کلیددهی با شمارهٔ موبایل | ۲ | ✅ ۲۰۰ |
| `social/dm/<کلید>.json` | **متن پیام‌های دایرکت** | ۱۲ | ✅ ۲۰۰ |
| `social/dm-idx/<شماره>.json` | فهرست گفتگوهای هر کاربر | ۵۴ | ✅ ۲۰۰ |
| `social/push/<شماره>.json` | اشتراک Web Push (endpoint) | ۷ | ✅ ۲۰۰ |
| `social/notif/<شماره>.json` | اعلان‌ها | ۸ | ✅ ۲۰۰ |

**چرا این از همهٔ کارهای فاز ۷A مهم‌تر است:** همهٔ آن IDORها را در لایهٔ API بستیم، ولی این
در ورودی دیگری باز است و اصلاً به API ما نمی‌رسد. نام فایل‌ها خودِ شمارهٔ موبایل است، پس
فهرست کامل شماره‌های کاربران هم قابل استخراج است. آدرس پروژه و نام باکت هر دو در باندل
عمومی کلاینت هستند.

**نکتهٔ تسکین‌دهنده:** کد OTP با HMAC-SHA256 و کلید `JWT_SECRET` هش شده، پس خود کد از روی
فایل قابل بازسازی نیست. این یک تصمیم درست قدیمی بود که الان جلوی بدترین حالت را گرفته.

**نوشتن با anon:** مستقیم آزمون نشد (کلاسیفایر اجازهٔ نوشتن آزمایشی نداد و دور نزدم). ولی
شواهد قطعی است: `components/Stories.tsx` و `components/MediaUpload.tsx` با کلید `anon` از
مرورگر مستقیم آپلود می‌کنند و فایل‌هایشان در `social/stories/img/` و `social/media/vid/`
موجود است. یعنی سیاست INSERT برای `anon` باز است. `uploadFile` هم `upsert: true` دارد و
`file.name` کاربر را عیناً در مسیر می‌گذارد.

**راه درمان (دو مرحله‌ای — هیچ‌کدام اجرا نشد):**
1. **فوری:** سیاست‌های `storage.objects` برای `anon` بسته شوند (LIST/INSERT/UPDATE/DELETE).
   این جلوی شمارش انبوه و آپلود دلخواه را می‌گیرد. ولی چون آپلودهای کلاینت با همین کلید
   کار می‌کنند، باید هم‌زمان آپلود به یک مسیر سرورساید (با `actorOf` + بررسی مالکیت +
   sniff نوع + سقف حجم + نام فایل ساخت خودمان) منتقل شود.
2. **اصلی:** `social/*` و `documents/*` به یک باکت **خصوصی** بروند و با Signed URL سرو شوند.
   تا وقتی این‌ها در باکت عمومی‌اند، حتی با بستن anon هم مسیرشان قابل حدس است
   (`social/dm-idx/<شماره>.json`).

### 🟠 S2 (HIGH) — اعتبارسنجی آپلود فقط سمت کلاینت است
`accept="image/*"` و `f.type.startsWith('video/')` و سقف ۲۰۰MB همه در جاوااسکریپت‌اند و
آپلود مستقیم از مرورگر به Storage می‌رود — هرگز به سرور ما نمی‌رسد. یعنی: بدون بررسی MIME
سرورساید، بدون سقف حجم (باکت `file_size_limit: null`)، بدون کنترل پسوند، و `file.name`
خام در مسیر. با S1 یک بستهٔ واحد است و درمانش هم همان است.

### 🟡 S3 (MEDIUM) — `/api/bookings/[id]` بدون احراز هویت
هر کس UUID رزرو را داشته باشد `booking_reference`، مبلغ، تاریخ، ساعت‌ها و `clubId` را
می‌گیرد. UUID حدس‌زدنی نیست، پس عملاً «امنیت با گمنامی» است. صفحهٔ نتیجهٔ پرداخت هنگام
بازگشت از درگاه ممکن است نشست نداشته باشد — پس تصمیم آگاهانه بوده، ولی بهتر است وقتی
نشست هست مالکیت بررسی شود.

### 🟡 S4 (MEDIUM) — قفل حساب قابل استفاده برای آزار کاربر
قفل روی شماره‌ای که مهاجم می‌فرستد اعمال می‌شود، پس می‌توان حساب قربانی را با ۵ رمز غلط
تا ۴ ساعت بست و بی‌نهایت تکرار کرد. سقف موقت دقیقاً طبق خواستهٔ شما است (قفل دائمی
نداریم)، ولی اگر آزار دیده شد، راه‌حل معمول این است که قفلِ حساب فقط وقتی اعمال شود که
IP هم مشکوک باشد.

### 🟢 S5 (LOW) — بدون Content-Security-Policy
پنج هدر امنیتی دیگر اضافه شدند؛ CSP عمداً نه. پروژه پر از استایل اینلاین و `<style>` داخل
کامپوننت است و CSP سخت‌گیرانه بدون تست کامل صفحه‌ها را می‌شکند. مرحلهٔ بعد: `report-only`.

### 🟢 S6 (LOW) — فایل مردهٔ ردیابی‌شده در Git
`apps/web/app/profile/me/page.tsx.tmp.12856.16dad454d2d6` (۲۷KB) در ایندکس Git است —
باقی‌ماندهٔ یک ویرایش نیمه‌کاره. نسخهٔ قدیمیِ صفحه است با الگوی احراز هویت منسوخ
(`localStorage.getItem('token')`) و فهرست هاردکد استان‌ها (خلاف قاعدهٔ CLAUDE.md).
مسیر نمی‌سازد و جایی import نشده. **حذف نشد** — تصمیمش با شماست.

## یافته‌های عملکرد

* **هیچ استفاده‌ای از `next/image` نیست** — ۵۷ فایل `<img>` خام دارند. یعنی بدون resize
  خودکار، بدون WebP/AVIF، بدون srcset، بدون lazy-loading. `public/images` ۲۷ مگابایت است.
* بزرگ‌ترین‌ها: `ads/5.gif` ۱.۹MB، `clubs/wallpaper3.png` ۱.۰MB، `ads/1.gif` ۰.۹MB.
* `next.config.js` روی `/images/:path*` هدر `no-cache, must-revalidate` می‌گذارد — یعنی
  ۲۷ مگابایت تصویر در هر بازدید revalidate می‌شود. این عمدی بوده (کامیت `2d3f193`، برای
  تازه‌شدن پس از دیپلوی) پس **تغییرش ندادم**؛ راه درست، نام فایل هش‌دار یا `next/image` است.
* باندل: مجموع chunkها ۴.۲MB؛ بزرگ‌ترین تک‌chunk ۲۲۲KB.
* فونت: ۹ وزن IRANSansX (۷۴۴KB) با `font-display: swap` — درست. `<link rel="preload">`
  برای وزن اصلی نیست.
* دو ویدیوی hero: `hero.mp4` ۱.۴MB و `hero 1.mp4` ۵۹۶KB (نام دومی فاصله دارد).

## یافته‌های Responsive

جدول‌های عریض (`admin/finance` با `minWidth: 620`، `admin/advertising` با `460`) داخل
`overflowX: auto` هستند ✅. گریدهای `stats-grid`، `gphotos`، `gvideos`، `stats-4`،
`actions-row` همه Breakpoint دارند ✅. سه گرید بدون Breakpoint ماندند که در ۳۲۰px تنگ
می‌شوند (نه شکسته): `events/[id]:355` و `profile/role:565` (`repeat(4,1fr)`) و
`clubs/[id]:694` (`repeat(3,1fr)`). `profile/role` از همه تنگ‌تر است — ۸ کارت نقش در
۴ ستون یعنی ۷۲px برای هر کارت.

## یافته‌های SEO

از ۱۰۴ صفحه فقط **۳ تا** metadata دارند (`layout`، `privacy`، `terms`). یعنی همهٔ
صفحه‌های باشگاه/مربی/داور/محصول عنوان یکسان ریشه را می‌گیرند — برای سایتی که ماهیتش
دایرکتوری است، این بزرگ‌ترین ضعف SEO است. `robots.ts` و `sitemap.ts` اضافه شدند ولی
`generateMetadata` برای مسیرهای داینامیک کار جداگانه‌ای است (~۲۰ فایل).
نقشهٔ سایت هم فقط مسیرهای ثابت را دارد؛ افزودن مسیرهای داینامیک نیاز به اتصال به
دیتابیس در زمان build دارد که اگر قطع باشد کل build را می‌شکند.

## حکم نهایی

# ⛔ NO-GO

**تنها به‌خاطر S1.** هر سه کاری که این فاز قرار بود انجام دهد (بازیابی رمز، ضدِ
Brute Force، تغییر رمز) کامل و آزمون‌شده‌اند و بقیهٔ یافته‌ها قابل پذیرش یا کم‌خطرند.
ولی تا وقتی پیام‌های دایرکت، اعلان‌ها، اشتراک‌های Push، رکوردهای OTP و مدارک هویتی
داوران **بدون هیچ کلیدی از اینترنت خواندنی‌اند**، سایت نباید عمومی شود.

این یافته با کار فاز ۷A رفع نمی‌شود؛ لایه‌اش فرق دارد. تا زمان رفع S1 و S2، حکم
NO-GO می‌ماند.

## قدم بعد (پیشنهادی — منتظر دستور)

PHASE 9 — جداسازی Storage: باکت خصوصی برای `social/*` و `documents/*`، انتقال آپلودها
به مسیر سرورساید با بررسی مالکیت و نوع و حجم، Signed URL برای خواندن، و بستن سیاست‌های
`anon` روی `storage.objects`.
