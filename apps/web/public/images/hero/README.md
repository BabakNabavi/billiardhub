# Hero image slot

`hero-lounge.jpg` — تصویر اصلی هیروی صفحه‌ی اول (لانژ بیلیاردِ روشن و پریمیوم؛
پنجره‌های قوسی، نور روز، میزِ مدرن با توپ‌ها و چوب — مطابق رفرنس برند).

- رزولوشن پیشنهادی: ۲۴۰۰×۱۶۰۰ یا بزرگ‌تر، JPG بهینه‌شده (~۳۰۰–۵۰۰KB).
- تا وقتی این فایل اضافه نشده، هیرو به‌صورت خودکار به عکسِ جایگزینِ موجود
  (`/images/shop/Home_table.jpg`) برمی‌گردد (onError fallback در app/page.tsx).
- نام فایل را عوض نکنید؛ فقط فایل نهایی را با همین نام اینجا بگذارید.
