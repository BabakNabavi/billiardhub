import { redirect } from 'next/navigation'

/* `/profile` هیچ‌وقت وجود نداشت — فقط زیرمسیرهایش (`me`, `role`, …).
   ولی چند جای داشبورد دکمه‌ی «تکمیل پروفایل» به همین نشانی لینک
   می‌داد و کاربر به صفحه‌ی خطا می‌خورد.

   مقصد `me` است چون همان صفحه‌ی ویرایش است و همه‌ی فیلدهای اختیاری
   (بیوگرافی، استان و شهر، جنسیت، اینستاگرام، آدرس، تلفنِ محلِ کار،
   رمز عبور و شماره‌ی موبایل) را یک‌جا دارد. */
export default function ProfileIndex() {
  redirect('/profile/me')
}
