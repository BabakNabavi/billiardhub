import { redirect } from 'next/navigation';

/* «رویدادها» یک بخش کاملاً ساختگی بود: فهرست هاردکد لیگ‌ها و
   قهرمانی‌هایی که وجود نداشتند، با صفحه‌ی جزئیات ساختگی‌تر (جدول
   گروهی، براکت و جوایز جعلی).

   همان مفهوم را حالا /tournaments با داده‌ی واقعی پوشش می‌دهد، پس
   به‌جای حذف نشانی — که برای لینک‌های بیرونی ۴۰۴ می‌شد — به آن‌جا
   هدایت می‌شود. */
export default function EventsPage() {
  redirect('/tournaments');
}
